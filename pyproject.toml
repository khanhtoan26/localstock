[project]
name = "localstock-workspace"
version = "1.0.0"
description = "LocalStock Monorepo — AI Stock Agent for Vietnamese Market"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["apps/prometheus"]
