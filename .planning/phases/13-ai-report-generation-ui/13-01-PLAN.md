---
phase: 13-ai-report-generation-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/helios/src/components/ui/sheet.tsx
  - apps/helios/src/components/ui/progress.tsx
  - apps/helios/src/app/globals.css
  - apps/helios/messages/en.json
  - apps/helios/messages/vi.json
autonomous: true
requirements: [ADMIN-08]

must_haves:
  truths:
    - "shadcn Sheet component installed and importable"
    - "shadcn Progress component installed and importable"
    - "step-active-pulse CSS animation available for use"
    - "All admin.report.* i18n keys present in en.json and vi.json"
  artifacts:
    - path: "apps/helios/src/components/ui/sheet.tsx"
      provides: "Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription, SheetFooter exports"
      contains: "SheetContent"
    - path: "apps/helios/src/components/ui/progress.tsx"
      provides: "Progress, ProgressTrack, ProgressIndicator exports"
      contains: "ProgressIndicator"
    - path: "apps/helios/src/app/globals.css"
      provides: "step-active-pulse animation class"
      contains: "step-active-pulse"
    - path: "apps/helios/messages/en.json"
      provides: "English i18n keys for report generation UI"
      contains: "admin.report"
    - path: "apps/helios/messages/vi.json"
      provides: "Vietnamese i18n keys for report generation UI"
      contains: "admin.report"
  key_links:
    - from: "apps/helios/src/app/globals.css"
      to: "report-progress.tsx (Plan 02)"
      via: "step-active-pulse class"
      pattern: "step-active-pulse"
---

<objective>
Install shadcn Sheet and Progress UI primitives, add the step-pulse CSS animation, and add all i18n keys for Phase 13 report generation UI.

Purpose: Provides the UI foundation (primitives + i18n + animation) that Plan 02 builds on. Without these, the 3 new components cannot be implemented.
Output: sheet.tsx, progress.tsx installed; globals.css updated with animation; en.json/vi.json updated with admin.report.* keys.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-report-generation-ui/13-CONTEXT.md
@.planning/phases/13-ai-report-generation-ui/13-RESEARCH.md
@.planning/phases/13-ai-report-generation-ui/13-UI-SPEC.md
@.planning/phases/13-ai-report-generation-ui/13-PATTERNS.md

<interfaces>
<!-- Existing globals.css animation pattern (append after this): -->
From apps/helios/src/app/globals.css (end of file):
```css
/* Job row highlight animation for admin job focus (Phase 12.1) */
@keyframes job-highlight {
  0%   { background-color: color-mix(in srgb, var(--primary) 8%, transparent); }
  13%  { background-color: color-mix(in srgb, var(--primary) 8%, transparent); }
  100% { background-color: transparent; }
}

.job-row-highlight {
  animation: job-highlight 2300ms ease-out forwards;
}
```

<!-- Existing admin i18n structure (en.json admin section keys): -->
```json
{
  "admin": {
    "title": "Admin Console",
    "tabs": { "stocks": "...", "pipeline": "...", "jobs": "..." },
    "stocks": { ... },
    "pipeline": { "crawl": "Crawl", "report": "Report", ... },
    "jobs": { ... },
    "toast": { "operationStarted": "...", "jobCompleted": "...", ... }
  }
}
```

<!-- shadcn install note from RESEARCH.md: -->
Current environment requires NODE_TLS_REJECT_UNAUTHORIZED=0 for shadcn registry access.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Sheet + Progress and add CSS animation</name>
  <read_first>
    - apps/helios/src/app/globals.css
    - apps/helios/components.json
  </read_first>
  <files>
    apps/helios/src/components/ui/sheet.tsx,
    apps/helios/src/components/ui/progress.tsx,
    apps/helios/src/app/globals.css
  </files>
  <action>
1. Install shadcn Sheet and Progress components:
```bash
cd apps/helios
NODE_TLS_REJECT_UNAUTHORIZED=0 npx shadcn@latest add sheet progress
```
This generates `src/components/ui/sheet.tsx` and `src/components/ui/progress.tsx` using @base-ui/react primitives (base-nova style preset per components.json).

2. Append the step-pulse animation to the END of `apps/helios/src/app/globals.css` (after the existing `.job-row-highlight` block):
```css

/* Step pulse animation for report generation progress (Phase 13) */
@keyframes step-pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.step-active-pulse {
  animation: step-pulse 2000ms ease-in-out infinite;
}
```

3. Verify the installed Sheet exports `Sheet`, `SheetContent`, `SheetHeader`, `SheetTitle`, `SheetDescription`, `SheetFooter` and Progress exports `Progress`, `ProgressTrack`, `ProgressIndicator`.
  </action>
  <verify>
    <automated>cd apps/helios && grep -l "SheetContent" src/components/ui/sheet.tsx && grep -l "ProgressIndicator" src/components/ui/progress.tsx && grep "step-active-pulse" src/app/globals.css && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/helios/src/components/ui/sheet.tsx exists and contains "SheetContent"
    - apps/helios/src/components/ui/sheet.tsx contains "SheetHeader"
    - apps/helios/src/components/ui/sheet.tsx contains "SheetFooter"
    - apps/helios/src/components/ui/progress.tsx exists and contains "ProgressIndicator"
    - apps/helios/src/components/ui/progress.tsx contains "ProgressTrack"
    - apps/helios/src/app/globals.css contains "@keyframes step-pulse"
    - apps/helios/src/app/globals.css contains ".step-active-pulse"
    - apps/helios/src/app/globals.css contains "animation: step-pulse 2000ms ease-in-out infinite"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sheet and Progress components installed from shadcn registry; step-active-pulse animation appended to globals.css; TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n keys for report generation UI</name>
  <read_first>
    - apps/helios/messages/en.json
    - apps/helios/messages/vi.json
  </read_first>
  <files>
    apps/helios/messages/en.json,
    apps/helios/messages/vi.json
  </files>
  <action>
1. Add the `report` key as a NEW nested object inside the `admin` object in `apps/helios/messages/en.json`. Place it after the `toast` key. Exact content:

```json
"report": {
  "sheetTitle": "Generate Report",
  "sheetDescriptionSingle": "Generating AI report for {symbol}",
  "sheetDescriptionBatch": "Generating AI reports for {count} stocks",
  "stepQueued": "Queued",
  "stepGenerating": "Generating report...",
  "stepComplete": "Report generated",
  "stepFailed": "Generation failed",
  "batchProgress": "{completed} of {total} stocks completed",
  "viewStockPage": "View stock page",
  "retry": "Retry",
  "close": "Close",
  "errorHeading": "Report generation failed",
  "errorOllamaOffline": "Ollama may be offline or unresponsive. Check that Ollama is running on localhost:11434.",
  "errorTimeout": "Generation timed out. The LLM model may be overloaded. Try again with fewer stocks.",
  "errorGeneric": "An unexpected error occurred. Check the Jobs tab for details.",
  "emptyPreview": "Report will appear here after generation completes.",
  "generatingFor": "Generating: {symbol}"
}
```

2. Add the matching `report` key inside `admin` in `apps/helios/messages/vi.json`. Exact content:

```json
"report": {
  "sheetTitle": "Tạo báo cáo",
  "sheetDescriptionSingle": "Đang tạo báo cáo AI cho {symbol}",
  "sheetDescriptionBatch": "Đang tạo báo cáo AI cho {count} mã",
  "stepQueued": "Đang chờ",
  "stepGenerating": "Đang tạo báo cáo...",
  "stepComplete": "Đã tạo xong",
  "stepFailed": "Tạo thất bại",
  "batchProgress": "Hoàn tất {completed}/{total} mã",
  "viewStockPage": "Xem trang cổ phiếu",
  "retry": "Thử lại",
  "close": "Đóng",
  "errorHeading": "Tạo báo cáo thất bại",
  "errorOllamaOffline": "Ollama có thể đang offline. Kiểm tra Ollama đang chạy tại localhost:11434.",
  "errorTimeout": "Quá thời gian chờ. LLM có thể đang quá tải. Thử lại với ít mã hơn.",
  "errorGeneric": "Đã xảy ra lỗi. Kiểm tra tab Tác vụ để xem chi tiết.",
  "emptyPreview": "Báo cáo sẽ hiển thị ở đây sau khi tạo xong.",
  "generatingFor": "Đang tạo: {symbol}"
}
```

3. Ensure the JSON remains valid after edits (no trailing commas, proper nesting).
  </action>
  <verify>
    <automated>cd apps/helios && node -e "const en=require('./messages/en.json'); const vi=require('./messages/vi.json'); const keys=['sheetTitle','sheetDescriptionSingle','sheetDescriptionBatch','stepQueued','stepGenerating','stepComplete','stepFailed','batchProgress','viewStockPage','retry','close','errorHeading','errorOllamaOffline','errorTimeout','errorGeneric','emptyPreview','generatingFor']; const missing=[]; keys.forEach(k=>{if(!en.admin.report[k])missing.push('en:'+k); if(!vi.admin.report[k])missing.push('vi:'+k)}); if(missing.length){console.error('Missing keys:',missing);process.exit(1)}else{console.log('All 17 i18n keys present in both en.json and vi.json')}"</automated>
  </verify>
  <acceptance_criteria>
    - apps/helios/messages/en.json contains "admin.report.sheetTitle" = "Generate Report"
    - apps/helios/messages/en.json contains "admin.report.stepQueued" = "Queued"
    - apps/helios/messages/en.json contains "admin.report.errorOllamaOffline" (non-empty string)
    - apps/helios/messages/en.json contains "admin.report.generatingFor" with "{symbol}" placeholder
    - apps/helios/messages/vi.json contains "admin.report.sheetTitle" = "Tạo báo cáo"
    - apps/helios/messages/vi.json contains "admin.report.stepQueued" = "Đang chờ"
    - apps/helios/messages/vi.json contains "admin.report.errorOllamaOffline" (non-empty string)
    - apps/helios/messages/vi.json contains "admin.report.generatingFor" with "{symbol}" placeholder
    - Both JSON files parse without errors: `node -e "require('./messages/en.json'); require('./messages/vi.json')"`
    - Total of 17 keys under admin.report in each file
  </acceptance_criteria>
  <done>All 17 admin.report.* i18n keys present in both en.json and vi.json with correct English and Vietnamese translations per UI-SPEC copywriting contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan installs UI primitives and adds static i18n/CSS — no data flow or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | i18n JSON files | accept | i18n keys are static display strings with no executable content. Placeholder interpolation ({symbol}, {count}) is handled by next-intl which escapes HTML by default. No user-supplied input reaches these strings. Low risk. |
| T-13-02 | Information Disclosure | shadcn components | accept | Sheet and Progress are UI primitives with no data handling. They render content provided by parent components. No secrets or sensitive data involved. |
</threat_model>

<verification>
1. `cd apps/helios && npx tsc --noEmit` — TypeScript compiles cleanly
2. `cd apps/helios && ls src/components/ui/sheet.tsx src/components/ui/progress.tsx` — both files exist
3. `cd apps/helios && grep "step-active-pulse" src/app/globals.css` — animation present
4. i18n validation script from Task 2 verify — all 17 keys in both locales
</verification>

<success_criteria>
- Sheet and Progress shadcn components are installed and type-check passes
- step-active-pulse CSS animation is defined in globals.css
- All 17 admin.report.* i18n keys exist in both en.json and vi.json with correct copy
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-report-generation-ui/13-01-SUMMARY.md`
</output>
