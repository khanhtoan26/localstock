---
phase: 09-academic-learning-page-glossary-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/helios/src/lib/glossary.ts
  - apps/helios/src/components/layout/sidebar.tsx
  - apps/helios/src/components/ui/input.tsx
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
user_setup: []

must_haves:
  truths:
    - "Glossary data module exports ≥25 typed entries across 3 categories (technical, fundamental, macro)"
    - "normalizeForSearch handles Vietnamese diacritics including Đ/đ → d"
    - "Sidebar shows 'Học' navigation item with BookOpen icon linking to /learn"
    - "Input shadcn component is installed and available for search UI"
  artifacts:
    - path: "apps/helios/src/lib/glossary.ts"
      provides: "Typed glossary data module — single source of truth"
      exports: ["GlossaryCategory", "GlossaryEntry", "glossary", "getEntriesByCategory", "getAllEntries", "normalizeForSearch"]
      contains: "export const glossary: Record<string, GlossaryEntry>"
    - path: "apps/helios/src/components/layout/sidebar.tsx"
      provides: "Sidebar with Học nav item"
      contains: "BookOpen"
    - path: "apps/helios/src/components/ui/input.tsx"
      provides: "Shadcn Input component for search"
  key_links:
    - from: "apps/helios/src/lib/glossary.ts"
      to: "Phase 10 glossary linking"
      via: "aliases field on each entry"
      pattern: "aliases:"
    - from: "apps/helios/src/components/layout/sidebar.tsx"
      to: "/learn"
      via: "navItems array href"
      pattern: 'href: "/learn"'
---

<objective>
Create the glossary data module (single source of truth for all learn pages and Phase 10 linking) and add sidebar navigation to /learn.

Purpose: The glossary data module is the foundation that all learn pages, search, and Phase 10 auto-linking depend on. The sidebar nav item makes the learn section discoverable. The Input component is needed for search UI in Plan 02.

Output: `src/lib/glossary.ts` with 25 typed entries + helpers, sidebar "Học" nav item, Input component installed.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-academic-learning-page-glossary-data/09-CONTEXT.md
@.planning/phases/09-academic-learning-page-glossary-data/09-RESEARCH.md
@.planning/phases/09-academic-learning-page-glossary-data/09-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/helios/src/components/layout/sidebar.tsx (current state):
```typescript
"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { BarChart3, Globe } from "lucide-react";
import { cn } from "@/lib/utils";

const navItems = [
  { href: "/rankings", label: "Xếp Hạng", icon: BarChart3 },
  { href: "/market", label: "Thị Trường", icon: Globe },
];
```

From apps/helios/src/lib/types.ts (pattern reference — named exports, interfaces):
```typescript
export interface StockScore {
  symbol: string;
  date: string;
  total_score: number;
  // ...
}
```

From apps/helios/src/components/ui/badge.tsx:
```typescript
export { Badge, badgeVariants }
```

From apps/helios/src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }
```

From apps/helios/src/components/ui/collapsible.tsx:
```typescript
export { Collapsible, CollapsibleTrigger, CollapsibleContent }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create glossary data module with 25 typed entries and helpers</name>
  <files>apps/helios/src/lib/glossary.ts</files>
  <read_first>
    - apps/helios/src/lib/types.ts (pattern reference for TypeScript interfaces and named exports)
    - .planning/phases/09-academic-learning-page-glossary-data/09-UI-SPEC.md §Glossary Data Model Contract (exact type definitions)
    - .planning/phases/09-academic-learning-page-glossary-data/09-RESEARCH.md §Pattern 2 and §Pattern 4 (data module and search normalization patterns)
  </read_first>
  <action>
Create `apps/helios/src/lib/glossary.ts` with the following structure:

**1. Type definitions (per D-06, UI-SPEC contract):**

```typescript
export type GlossaryCategory = "technical" | "fundamental" | "macro";

export interface GlossaryEntry {
  id: string;                    // URL-safe slug: "rsi", "pe-ratio", "cpi"
  term: string;                  // Vietnamese name: "Chỉ số sức mạnh tương đối (RSI)"
  termEn: string;                // English name: "Relative Strength Index"
  aliases: string[];             // Phase 10 matching: ["RSI", "chỉ số RSI", "Relative Strength Index"]
  category: GlossaryCategory;
  shortDef: string;              // 1-sentence Vietnamese definition
  content: string;               // Full markdown article (Vietnamese, English terms in parens)
  formula?: string;              // Optional: plain text formula
}
```

**2. Helper functions:**

```typescript
export function getEntriesByCategory(category: GlossaryCategory): GlossaryEntry[] {
  return Object.values(glossary).filter((e) => e.category === category);
}

export function getAllEntries(): GlossaryEntry[] {
  return Object.values(glossary);
}

export function normalizeForSearch(text: string): string {
  return text
    .normalize("NFD")
    .replace(/[\u0300-\u036f]/g, "")  // Remove combining diacritical marks
    .replace(/[đĐ]/g, "d")            // Vietnamese đ/Đ → d (NOT handled by NFD)
    .toLowerCase();
}
```

**CRITICAL for normalizeForSearch:** The `.replace(/[đĐ]/g, "d")` step is MANDATORY — NFD decomposition does NOT handle the Vietnamese character Đ (U+0110). Without it, searching "duong" won't match "Đường".

**3. Glossary entries** — `export const glossary: Record<string, GlossaryEntry> = { ... }`

Create exactly 25 entries. Each entry's `content` field must be a detailed Vietnamese article (per D-01) with the following markdown structure:
- `## Định nghĩa` — What the indicator/ratio is
- `## Cách tính` (if formula exists) — How it's calculated
- `## Cách đọc / Diễn giải` — How to interpret values
- `## Ví dụ thực tế` — Practical example with Vietnamese stock context
- `## Lưu ý` — Important notes/caveats

Content language: Vietnamese with English technical terms in parentheses (per D-02). Example: "RSI (Relative Strength Index) đo lường..."

**Complete entry list (all 25):**

**Technical category (10 entries):**

| id | term | termEn | aliases | formula |
|---|---|---|---|---|
| `rsi` | "Chỉ số sức mạnh tương đối (RSI)" | "Relative Strength Index" | ["RSI", "chỉ số RSI", "Relative Strength Index", "chỉ số sức mạnh tương đối"] | "RSI = 100 - (100 / (1 + RS)), RS = Avg Gain / Avg Loss (14 phiên)" |
| `macd` | "Đường MACD" | "Moving Average Convergence Divergence" | ["MACD", "hội tụ phân kỳ", "Moving Average Convergence Divergence"] | "MACD = EMA(12) - EMA(26), Signal = EMA(9) của MACD" |
| `sma` | "Đường trung bình đơn giản (SMA)" | "Simple Moving Average" | ["SMA", "đường trung bình", "Simple Moving Average", "MA"] | "SMA(n) = (P1 + P2 + ... + Pn) / n" |
| `ema` | "Đường trung bình hàm mũ (EMA)" | "Exponential Moving Average" | ["EMA", "trung bình hàm mũ", "Exponential Moving Average"] | "EMA = Price × k + EMA(previous) × (1 - k), k = 2 / (n + 1)" |
| `bollinger-bands` | "Dải Bollinger (Bollinger Bands)" | "Bollinger Bands" | ["Bollinger Bands", "dải Bollinger", "BB", "Bollinger"] | "Middle = SMA(20), Upper = SMA(20) + 2σ, Lower = SMA(20) - 2σ" |
| `obv` | "Khối lượng cân bằng (OBV)" | "On-Balance Volume" | ["OBV", "On-Balance Volume", "khối lượng cân bằng"] | "OBV = OBV(trước) ± Volume (+ nếu giá tăng, - nếu giá giảm)" |
| `vwap` | "Giá trung bình theo khối lượng (VWAP)" | "Volume Weighted Average Price" | ["VWAP", "Volume Weighted Average Price", "giá trung bình theo khối lượng"] | "VWAP = Σ(Price × Volume) / Σ(Volume)" |
| `stochastic` | "Stochastic Oscillator" | "Stochastic Oscillator" | ["Stochastic", "%K", "%D", "Stochastic Oscillator"] | "%K = (Close - Low14) / (High14 - Low14) × 100, %D = SMA(3) của %K" |
| `adx` | "Chỉ số xu hướng trung bình (ADX)" | "Average Directional Index" | ["ADX", "Average Directional Index", "chỉ số xu hướng"] | "ADX = SMA(14) của DX, DX = |+DI - -DI| / (+DI + -DI) × 100" |
| `atr` | "Phạm vi thực trung bình (ATR)" | "Average True Range" | ["ATR", "Average True Range", "phạm vi thực"] | "ATR = SMA(14) của True Range, TR = max(H-L, |H-Cprev|, |L-Cprev|)" |

**Fundamental category (10 entries):**

| id | term | termEn | aliases | formula |
|---|---|---|---|---|
| `pe-ratio` | "Hệ số giá trên lợi nhuận (P/E)" | "Price-to-Earnings Ratio" | ["P/E", "PE", "Price-to-Earnings", "hệ số giá trên lợi nhuận"] | "P/E = Giá cổ phiếu / EPS" |
| `pb-ratio` | "Hệ số giá trên giá trị sổ sách (P/B)" | "Price-to-Book Ratio" | ["P/B", "PB", "Price-to-Book", "giá trên giá trị sổ sách"] | "P/B = Giá cổ phiếu / Giá trị sổ sách mỗi cổ phiếu" |
| `eps` | "Lợi nhuận trên mỗi cổ phiếu (EPS)" | "Earnings Per Share" | ["EPS", "Earnings Per Share", "lợi nhuận trên cổ phiếu"] | "EPS = (Lợi nhuận ròng - Cổ tức ưu đãi) / Số cổ phiếu lưu hành" |
| `roe` | "Tỷ suất lợi nhuận trên vốn chủ sở hữu (ROE)" | "Return on Equity" | ["ROE", "Return on Equity", "lợi nhuận trên vốn chủ"] | "ROE = Lợi nhuận ròng / Vốn chủ sở hữu × 100%" |
| `roa` | "Tỷ suất lợi nhuận trên tổng tài sản (ROA)" | "Return on Assets" | ["ROA", "Return on Assets", "lợi nhuận trên tổng tài sản"] | "ROA = Lợi nhuận ròng / Tổng tài sản × 100%" |
| `de-ratio` | "Hệ số nợ trên vốn chủ sở hữu (D/E)" | "Debt-to-Equity Ratio" | ["D/E", "DE", "Debt-to-Equity", "hệ số nợ"] | "D/E = Tổng nợ / Vốn chủ sở hữu" |
| `revenue-growth` | "Tăng trưởng doanh thu" | "Revenue Growth" | ["tăng trưởng doanh thu", "Revenue Growth", "doanh thu"] | "Revenue Growth = (Doanh thu kỳ này - Doanh thu kỳ trước) / Doanh thu kỳ trước × 100%" |
| `profit-growth` | "Tăng trưởng lợi nhuận" | "Profit Growth" | ["tăng trưởng lợi nhuận", "Profit Growth", "lợi nhuận"] | "Profit Growth = (LN kỳ này - LN kỳ trước) / LN kỳ trước × 100%" |
| `market-cap` | "Vốn hóa thị trường" | "Market Capitalization" | ["vốn hóa", "Market Cap", "Market Capitalization", "vốn hóa thị trường"] | "Market Cap = Giá cổ phiếu × Số cổ phiếu lưu hành" |
| `current-ratio` | "Hệ số thanh toán hiện hành" | "Current Ratio" | ["Current Ratio", "hệ số thanh toán", "thanh toán hiện hành"] | "Current Ratio = Tài sản ngắn hạn / Nợ ngắn hạn" |

**Macro category (5 entries):**

| id | term | termEn | aliases | formula |
|---|---|---|---|---|
| `cpi` | "Chỉ số giá tiêu dùng (CPI)" | "Consumer Price Index" | ["CPI", "Consumer Price Index", "chỉ số giá tiêu dùng", "lạm phát"] | undefined (no formula) |
| `gdp-growth` | "Tăng trưởng GDP" | "GDP Growth Rate" | ["GDP", "GDP Growth", "tăng trưởng GDP", "tổng sản phẩm quốc nội"] | "GDP Growth = (GDP kỳ này - GDP kỳ trước) / GDP kỳ trước × 100%" |
| `interest-rate` | "Lãi suất điều hành" | "Policy Interest Rate" | ["lãi suất", "Interest Rate", "lãi suất điều hành", "lãi suất chính sách"] | undefined (no formula) |
| `exchange-rate` | "Tỷ giá hối đoái" | "Exchange Rate" | ["tỷ giá", "Exchange Rate", "USD/VND", "tỷ giá hối đoái"] | undefined (no formula) |
| `money-supply-m2` | "Cung tiền M2" | "Money Supply M2" | ["M2", "Money Supply", "cung tiền", "cung tiền M2"] | undefined (no formula) |

**shortDef examples** (1 sentence, Vietnamese):
- RSI: "Chỉ báo dao động đo lường tốc độ và biên độ thay đổi giá, giúp xác định trạng thái quá mua hoặc quá bán."
- P/E: "Hệ số cho biết nhà đầu tư sẵn sàng trả bao nhiêu đồng cho mỗi đồng lợi nhuận của doanh nghiệp."
- CPI: "Chỉ số đo lường mức thay đổi giá cả hàng hóa và dịch vụ tiêu dùng theo thời gian, phản ánh lạm phát."

Write each shortDef as a single Vietnamese sentence explaining what the indicator measures or means.

**content field:** Each entry's content must be 150-400 words of Vietnamese educational text following the section structure above. Use template literals (backtick strings) for multiline content. Organize entries in the file by category with section comments: `// === TECHNICAL INDICATORS ===`, `// === FUNDAMENTAL RATIOS ===`, `// === MACRO CONCEPTS ===`.
  </action>
  <verify>
    <automated>cd apps/helios && npx tsx --eval "
import { glossary, getAllEntries, getEntriesByCategory, normalizeForSearch } from './src/lib/glossary';
const all = getAllEntries();
const tech = getEntriesByCategory('technical');
const fund = getEntriesByCategory('fundamental');
const macro = getEntriesByCategory('macro');
const n1 = normalizeForSearch('chỉ số');
const n2 = normalizeForSearch('chi so');
const n3 = normalizeForSearch('Đường');
const n4 = normalizeForSearch('duong');
console.log('Total entries:', all.length, all.length >= 25 ? 'PASS' : 'FAIL');
console.log('Technical:', tech.length, tech.length >= 10 ? 'PASS' : 'FAIL');
console.log('Fundamental:', fund.length, fund.length >= 10 ? 'PASS' : 'FAIL');
console.log('Macro:', macro.length, macro.length >= 5 ? 'PASS' : 'FAIL');
console.log('Diacritic match:', n1 === n2 ? 'PASS' : 'FAIL');
console.log('Đ handling:', n3 === n4 ? 'PASS' : 'FAIL', n3, n4);
console.log('All have content:', all.every(e => e.content.length > 100) ? 'PASS' : 'FAIL');
console.log('All have aliases:', all.every(e => e.aliases.length > 0) ? 'PASS' : 'FAIL');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File `apps/helios/src/lib/glossary.ts` exists
    - Contains `export type GlossaryCategory = "technical" | "fundamental" | "macro"`
    - Contains `export interface GlossaryEntry`
    - Contains `export const glossary: Record<string, GlossaryEntry>`
    - Contains `export function getEntriesByCategory`
    - Contains `export function getAllEntries`
    - Contains `export function normalizeForSearch`
    - Contains `.replace(/[đĐ]/g, "d")` in normalizeForSearch
    - Contains `.normalize("NFD")` in normalizeForSearch
    - Total entries ≥ 25 (grep count of `id:` occurrences)
    - Every entry has non-empty `content` field (>100 chars)
    - Every entry has non-empty `aliases` array
    - `cd apps/helios && npm run build` passes with no TypeScript errors
  </acceptance_criteria>
  <done>Glossary data module is the single source of truth with ≥25 typed entries, 3 helper functions, and diacritic-insensitive search normalization. All entries have detailed Vietnamese educational content.</done>
</task>

<task type="auto">
  <name>Task 2: Add sidebar navigation and install Input component</name>
  <files>apps/helios/src/components/layout/sidebar.tsx, apps/helios/src/components/ui/input.tsx</files>
  <read_first>
    - apps/helios/src/components/layout/sidebar.tsx (current navItems array — must see exact structure to add entry)
    - .planning/phases/09-academic-learning-page-glossary-data/09-UI-SPEC.md §Component Inventory (Input install command, sidebar change)
    - .planning/phases/09-academic-learning-page-glossary-data/09-CONTEXT.md §D-10 (exact nav item spec: "Học" label, BookOpen icon)
  </read_first>
  <action>
**Step 1: Install shadcn Input component**

Run: `cd apps/helios && npx shadcn@latest add input`

This installs `apps/helios/src/components/ui/input.tsx` — needed for search bar in Plan 02.

**Step 2: Add "Học" nav item to sidebar**

In `apps/helios/src/components/layout/sidebar.tsx`:

1. Add `BookOpen` to the lucide-react import:
   ```typescript
   import { BarChart3, Globe, BookOpen } from "lucide-react";
   ```

2. Add the learn nav item as the LAST entry in the `navItems` array:
   ```typescript
   const navItems = [
     { href: "/rankings", label: "Xếp Hạng", icon: BarChart3 },
     { href: "/market", label: "Thị Trường", icon: Globe },
     { href: "/learn", label: "Học", icon: BookOpen },
   ];
   ```

Per D-10: The label is exactly "Học", the icon is exactly `BookOpen`, the href is exactly `/learn`.
  </action>
  <verify>
    <automated>cd apps/helios && grep -q "BookOpen" src/components/layout/sidebar.tsx && grep -q '"/learn"' src/components/layout/sidebar.tsx && grep -q '"Học"' src/components/layout/sidebar.tsx && test -f src/components/ui/input.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/helios/src/components/layout/sidebar.tsx` imports `BookOpen` from `lucide-react`
    - `apps/helios/src/components/layout/sidebar.tsx` contains `{ href: "/learn", label: "Học", icon: BookOpen }`
    - `apps/helios/src/components/ui/input.tsx` exists (installed by shadcn)
    - `cd apps/helios && npm run build` passes
  </acceptance_criteria>
  <done>Sidebar shows "Học" with BookOpen icon linking to /learn. Input component is installed for Plan 02 search UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates a static data module and modifies sidebar navigation — no user input processing, no API calls, no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | glossary.ts content strings | accept | Content is author-controlled hardcoded TypeScript — not user-generated. No injection vector. |
</threat_model>

<verification>
1. `cd apps/helios && npm run build` — TypeScript compilation passes with new glossary module and sidebar change
2. `npx tsx` inline check confirms ≥25 entries, 3 categories, diacritic normalization works
3. Sidebar has 3 nav items (rankings, market, learn)
4. Input component file exists
</verification>

<success_criteria>
- Glossary data module (`src/lib/glossary.ts`) exports typed Record with ≥25 entries across 3 categories
- Each entry has detailed Vietnamese educational content (not just a definition)
- `normalizeForSearch` handles both standard diacritics AND Vietnamese Đ/đ
- Sidebar shows "Học" with BookOpen icon
- Input component installed
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-academic-learning-page-glossary-data/09-01-SUMMARY.md`
</output>
