---
phase: 09-academic-learning-page-glossary-data
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/helios/src/components/learn/glossary-entry-card.tsx
  - apps/helios/src/components/learn/glossary-search.tsx
  - apps/helios/src/app/learn/page.tsx
  - apps/helios/src/app/learn/[category]/page.tsx
  - apps/helios/src/app/globals.css
autonomous: false
requirements:
  - LEARN-01
  - LEARN-03
  - LEARN-04
user_setup: []

must_haves:
  truths:
    - "User can navigate to /learn and see 3 category cards (Technical, Fundamental, Macro)"
    - "User can click a category card and see entries for that category at /learn/[category]"
    - "Each entry shows Vietnamese term + English name + short definition, click expands to full article"
    - "User can search entries with diacritic-insensitive matching (typing 'chi so' finds 'chỉ số')"
    - "Invalid category slug (e.g., /learn/invalid) shows 404"
    - "Collapsible entry cards animate smoothly on expand/collapse"
  artifacts:
    - path: "apps/helios/src/app/learn/page.tsx"
      provides: "Learn hub page with 3 category cards"
      contains: "Học — Kiến Thức Đầu Tư"
    - path: "apps/helios/src/app/learn/[category]/page.tsx"
      provides: "Category page with Server Component shell"
      contains: "generateStaticParams"
    - path: "apps/helios/src/components/learn/glossary-entry-card.tsx"
      provides: "Expandable entry card with collapsible + react-markdown"
      exports: ["GlossaryEntryCard"]
    - path: "apps/helios/src/components/learn/glossary-search.tsx"
      provides: "Search input + filtered entry list"
      exports: ["GlossarySearch"]
  key_links:
    - from: "apps/helios/src/app/learn/page.tsx"
      to: "/learn/[category]"
      via: "Next.js Link component"
      pattern: "href={`/learn/"
    - from: "apps/helios/src/app/learn/[category]/page.tsx"
      to: "apps/helios/src/components/learn/glossary-search.tsx"
      via: "Server Component delegates to Client Component"
      pattern: "<GlossarySearch"
    - from: "apps/helios/src/components/learn/glossary-search.tsx"
      to: "apps/helios/src/components/learn/glossary-entry-card.tsx"
      via: "renders entry cards from filtered list"
      pattern: "<GlossaryEntryCard"
    - from: "apps/helios/src/components/learn/glossary-search.tsx"
      to: "apps/helios/src/lib/glossary.ts"
      via: "imports normalizeForSearch for diacritic-insensitive filtering"
      pattern: "normalizeForSearch"
---

<objective>
Build the learn pages (hub + category routing) and interactive client components (search + expandable entry cards) that consume the glossary data module from Plan 01.

Purpose: Deliver the user-facing educational experience — browsable, searchable glossary entries organized by category with full article content.

Output: Hub page at /learn, 3 category pages at /learn/[category], interactive search + expandable cards, collapsible CSS animation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-academic-learning-page-glossary-data/09-CONTEXT.md
@.planning/phases/09-academic-learning-page-glossary-data/09-RESEARCH.md
@.planning/phases/09-academic-learning-page-glossary-data/09-UI-SPEC.md
@.planning/phases/09-academic-learning-page-glossary-data/09-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From apps/helios/src/lib/glossary.ts (created in Plan 01):
```typescript
export type GlossaryCategory = "technical" | "fundamental" | "macro";

export interface GlossaryEntry {
  id: string;
  term: string;
  termEn: string;
  aliases: string[];
  category: GlossaryCategory;
  shortDef: string;
  content: string;
  formula?: string;
}

export const glossary: Record<string, GlossaryEntry> = { ... };
export function getEntriesByCategory(category: GlossaryCategory): GlossaryEntry[];
export function getAllEntries(): GlossaryEntry[];
export function normalizeForSearch(text: string): string;
```

From apps/helios/src/components/ui/card.tsx:
```typescript
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent }
// Card uses: className, size prop
// CardHeader: className
// CardTitle: className (renders <div> with font-heading text-base)
// CardDescription: className (renders <div> with text-sm text-muted-foreground)
// CardContent: className (renders <div> with px-4)
```

From apps/helios/src/components/ui/collapsible.tsx:
```typescript
// Wraps @base-ui/react Collapsible
export { Collapsible, CollapsibleTrigger, CollapsibleContent }
// Collapsible: Root.Props (open, onOpenChange, defaultOpen)
// CollapsibleTrigger: Trigger.Props (renders button, data-slot="collapsible-trigger")
// CollapsibleContent: Panel.Props (data-slot="collapsible-content")
```

From apps/helios/src/components/ui/badge.tsx:
```typescript
export { Badge, badgeVariants }
// Badge variants: "default" | "secondary" | "destructive" | "outline" | "ghost" | "link"
```

From apps/helios/src/components/ui/input.tsx (installed in Plan 01):
```typescript
export { Input }
// Standard shadcn Input with theme tokens
```

From apps/helios/src/components/layout/sidebar.tsx:
```typescript
// Already has { href: "/learn", label: "Học", icon: BookOpen } from Plan 01
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GlossaryEntryCard and GlossarySearch client components</name>
  <files>apps/helios/src/components/learn/glossary-entry-card.tsx, apps/helios/src/components/learn/glossary-search.tsx</files>
  <read_first>
    - apps/helios/src/lib/glossary.ts (type definitions and normalizeForSearch — must import these)
    - apps/helios/src/components/ui/collapsible.tsx (exact API: Collapsible, CollapsibleTrigger, CollapsibleContent)
    - apps/helios/src/components/ui/card.tsx (exact API: Card, CardHeader, CardTitle, CardDescription, CardContent)
    - apps/helios/src/components/ui/input.tsx (installed in Plan 01 — use for search input)
    - .planning/phases/09-academic-learning-page-glossary-data/09-UI-SPEC.md §Interaction Contract and §Typography (prose classes, search behavior, collapsible behavior)
    - .planning/phases/09-academic-learning-page-glossary-data/09-RESEARCH.md §Pattern 3 and §Code Examples > Client-Side Search Component
  </read_first>
  <action>
Create directory `apps/helios/src/components/learn/` then create two client components:

**File 1: `apps/helios/src/components/learn/glossary-entry-card.tsx`**

```typescript
"use client";

import { useState } from "react";
import { ChevronDown, ChevronUp } from "lucide-react";
import ReactMarkdown from "react-markdown";
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";
import { Collapsible, CollapsibleTrigger, CollapsibleContent } from "@/components/ui/collapsible";
import type { GlossaryEntry } from "@/lib/glossary";

interface GlossaryEntryCardProps {
  entry: GlossaryEntry;
  defaultOpen?: boolean;
}

export function GlossaryEntryCard({ entry, defaultOpen = false }: GlossaryEntryCardProps) {
  const [open, setOpen] = useState(defaultOpen);

  return (
    <Collapsible open={open} onOpenChange={setOpen}>
      <Card id={entry.id} className="scroll-mt-20">
        <CollapsibleTrigger
          className="w-full cursor-pointer text-left"
          aria-label={open ? `Thu gọn ${entry.term}` : `Mở rộng ${entry.term}`}
        >
          <CardHeader>
            <div className="flex items-center justify-between">
              <CardTitle className="text-base">{entry.term}</CardTitle>
              {open ? (
                <ChevronUp className="h-4 w-4 shrink-0 text-muted-foreground" />
              ) : (
                <ChevronDown className="h-4 w-4 shrink-0 text-muted-foreground" />
              )}
            </div>
            <CardDescription>{entry.shortDef}</CardDescription>
          </CardHeader>
        </CollapsibleTrigger>
        <CollapsibleContent>
          <CardContent>
            {entry.formula && (
              <div className="mb-4">
                <span className="text-xs text-muted-foreground">Công thức</span>
                <code className="block bg-muted rounded-md p-3 font-mono text-sm mt-1">
                  {entry.formula}
                </code>
              </div>
            )}
            <div className="prose prose-sm dark:prose-invert max-w-none prose-headings:text-foreground prose-p:text-foreground/90 prose-strong:text-foreground prose-code:text-muted-foreground">
              <ReactMarkdown>{entry.content}</ReactMarkdown>
            </div>
          </CardContent>
        </CollapsibleContent>
      </Card>
    </Collapsible>
  );
}
```

Key details:
- `"use client"` directive required (uses useState)
- `scroll-mt-20` on Card for deep-link scrolling (Phase 10)
- `aria-label` with Vietnamese text per UI-SPEC accessibility contract
- Prose classes exactly as specified in UI-SPEC §Typography > Article Content Typography
- Formula block with `bg-muted rounded-md p-3 font-mono text-sm` styling
- Named export `GlossaryEntryCard` (NOT default export)

**File 2: `apps/helios/src/components/learn/glossary-search.tsx`**

```typescript
"use client";

import { useState, useEffect } from "react";
import { Search, X } from "lucide-react";
import { Input } from "@/components/ui/input";
import { GlossaryEntryCard } from "./glossary-entry-card";
import { normalizeForSearch, type GlossaryEntry, type GlossaryCategory } from "@/lib/glossary";

// Category-specific search placeholders per UI-SPEC Copywriting Contract
const SEARCH_PLACEHOLDERS: Record<GlossaryCategory, string> = {
  technical: "Tìm chỉ báo kỹ thuật...",
  fundamental: "Tìm tỷ số cơ bản...",
  macro: "Tìm yếu tố vĩ mô...",
};

interface GlossarySearchProps {
  entries: GlossaryEntry[];
  category: GlossaryCategory;
}

export function GlossarySearch({ entries, category }: GlossarySearchProps) {
  const [query, setQuery] = useState("");
  const [hashId, setHashId] = useState<string | null>(null);

  // Read URL hash on mount for deep-link auto-expand (Phase 10 support)
  useEffect(() => {
    const hash = window.location.hash.slice(1);
    if (hash) {
      setHashId(hash);
      // Scroll to the matching entry after render
      const el = document.getElementById(hash);
      if (el) {
        setTimeout(() => el.scrollIntoView({ behavior: "smooth", block: "start" }), 100);
      }
    }
  }, []);

  const filtered = query
    ? entries.filter((entry) => {
        const normalized = normalizeForSearch(query);
        return (
          normalizeForSearch(entry.term).includes(normalized) ||
          normalizeForSearch(entry.termEn).includes(normalized) ||
          normalizeForSearch(entry.shortDef).includes(normalized) ||
          entry.aliases.some((alias) =>
            normalizeForSearch(alias).includes(normalized)
          )
        );
      })
    : entries;

  return (
    <div>
      {/* Search input */}
      <div className="relative">
        <label htmlFor={`search-${category}`} className="sr-only">
          {SEARCH_PLACEHOLDERS[category]}
        </label>
        <Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground pointer-events-none" />
        <Input
          id={`search-${category}`}
          type="text"
          value={query}
          onChange={(e) => setQuery(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === "Escape") {
              setQuery("");
              (e.target as HTMLInputElement).focus();
            }
          }}
          placeholder={SEARCH_PLACEHOLDERS[category]}
          className="h-10 w-full pl-10 pr-10"
        />
        {query && (
          <button
            onClick={() => setQuery("")}
            className="absolute right-3 top-1/2 -translate-y-1/2"
            aria-label="Xóa tìm kiếm"
          >
            <X className="h-4 w-4 text-muted-foreground" />
          </button>
        )}
      </div>

      {/* Result count */}
      <p className="text-xs text-muted-foreground mt-2" aria-live="polite">
        {filtered.length} kết quả
      </p>

      {/* Entry list */}
      <div className="mt-4 space-y-4">
        {filtered.length === 0 ? (
          <p className="text-sm text-muted-foreground py-8 text-center">
            Không tìm thấy kết quả cho &quot;{query}&quot;
          </p>
        ) : (
          filtered.map((entry) => (
            <GlossaryEntryCard
              key={entry.id}
              entry={entry}
              defaultOpen={entry.id === hashId}
            />
          ))
        )}
      </div>
    </div>
  );
}
```

Key details:
- `"use client"` directive required (uses useState, useEffect)
- Uses `Input` component from shadcn (installed in Plan 01) — NOT raw `<input>`
- `SEARCH_PLACEHOLDERS` record with exact Vietnamese text from UI-SPEC Copywriting Contract
- `sr-only` label for accessibility (WCAG 2.1 1.3.1)
- `aria-live="polite"` on result count for screen reader updates
- `aria-label="Xóa tìm kiếm"` on clear button
- Escape key clears search and re-focuses input
- Filters on `term`, `termEn`, `shortDef`, `aliases[]` using `normalizeForSearch`
- Hash-based auto-expand for Phase 10 deep links
- Named export `GlossarySearch` (NOT default export)
  </action>
  <verify>
    <automated>cd apps/helios && grep -q "export function GlossaryEntryCard" src/components/learn/glossary-entry-card.tsx && grep -q "export function GlossarySearch" src/components/learn/glossary-search.tsx && grep -q "normalizeForSearch" src/components/learn/glossary-search.tsx && grep -q 'aria-live="polite"' src/components/learn/glossary-search.tsx && grep -q "ReactMarkdown" src/components/learn/glossary-entry-card.tsx && grep -q "Collapsible" src/components/learn/glossary-entry-card.tsx && npm run build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/helios/src/components/learn/glossary-entry-card.tsx` exists
    - Contains `"use client"` directive
    - Contains `export function GlossaryEntryCard`
    - Contains `ReactMarkdown` import and usage
    - Contains `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent` from `@/components/ui/collapsible`
    - Contains `scroll-mt-20` on Card element
    - Contains `prose prose-sm dark:prose-invert max-w-none` class string
    - Contains `Công thức` label for formula block
    - File `apps/helios/src/components/learn/glossary-search.tsx` exists
    - Contains `"use client"` directive
    - Contains `export function GlossarySearch`
    - Contains `normalizeForSearch` import from `@/lib/glossary`
    - Contains `Input` import from `@/components/ui/input`
    - Contains `aria-live="polite"` on result count
    - Contains `aria-label="Xóa tìm kiếm"` on clear button
    - Contains `sr-only` label for search input accessibility
    - Contains `SEARCH_PLACEHOLDERS` with "Tìm chỉ báo kỹ thuật...", "Tìm tỷ số cơ bản...", "Tìm yếu tố vĩ mô..."
    - `cd apps/helios && npm run build` passes
  </acceptance_criteria>
  <done>Two client components created: GlossaryEntryCard (expandable card with markdown rendering) and GlossarySearch (diacritic-insensitive search + filtered entry list). Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create Learn hub page, category page routing, and collapsible CSS</name>
  <files>apps/helios/src/app/learn/page.tsx, apps/helios/src/app/learn/[category]/page.tsx, apps/helios/src/app/globals.css</files>
  <read_first>
    - apps/helios/src/app/globals.css (current state — add collapsible CSS rules at the end)
    - apps/helios/src/app/layout.tsx (app layout pattern — ensure learn pages inherit layout)
    - apps/helios/src/lib/glossary.ts (import getEntriesByCategory, GlossaryCategory for page data)
    - apps/helios/src/components/learn/glossary-search.tsx (import GlossarySearch for category page)
    - .planning/phases/09-academic-learning-page-glossary-data/09-UI-SPEC.md §Page Structure & Layout (hub wireframe, category wireframe, responsive breakpoints)
    - .planning/phases/09-academic-learning-page-glossary-data/09-UI-SPEC.md §Copywriting Contract (exact Vietnamese strings)
    - .planning/phases/09-academic-learning-page-glossary-data/09-RESEARCH.md §Pattern 1 (Server Component shell + Client wrapper pattern)
    - .planning/phases/09-academic-learning-page-glossary-data/09-RESEARCH.md §Pitfall 2 (Next.js 16 params is Promise — must await)
  </read_first>
  <action>
**File 1: `apps/helios/src/app/learn/page.tsx`** (Server Component — NO `"use client"`)

Create directory `apps/helios/src/app/learn/` first.

```typescript
import Link from "next/link";
import { TrendingUp, Calculator, Globe } from "lucide-react";
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { getEntriesByCategory } from "@/lib/glossary";

const categories = [
  {
    slug: "technical" as const,
    title: "Chỉ Báo Kỹ Thuật",
    titleEn: "Technical Indicators",
    desc: "Các chỉ báo phân tích xu hướng và động lượng giá cổ phiếu",
    icon: TrendingUp,
  },
  {
    slug: "fundamental" as const,
    title: "Tỷ Số Cơ Bản",
    titleEn: "Fundamental Ratios",
    desc: "Các chỉ số đánh giá sức khỏe tài chính và định giá doanh nghiệp",
    icon: Calculator,
  },
  {
    slug: "macro" as const,
    title: "Yếu Tố Vĩ Mô",
    titleEn: "Macro Concepts",
    desc: "Các chỉ số kinh tế vĩ mô ảnh hưởng đến thị trường chứng khoán",
    icon: Globe,
  },
] as const;

export default function LearnPage() {
  return (
    <div className="max-w-3xl mx-auto">
      <h1 className="text-2xl font-semibold">Học — Kiến Thức Đầu Tư</h1>
      <p className="text-sm text-muted-foreground mt-2">
        Tìm hiểu các chỉ báo kỹ thuật, tỷ số cơ bản và yếu tố vĩ mô được sử dụng trong phân tích AI
      </p>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">
        {categories.map(({ slug, title, titleEn, desc, icon: Icon }) => {
          const count = getEntriesByCategory(slug).length;
          return (
            <Link key={slug} href={`/learn/${slug}`} className="focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring rounded-xl">
              <Card className="h-full hover:ring-2 hover:ring-primary/20 hover:shadow-md transition-shadow cursor-pointer">
                <CardHeader>
                  <Icon className="h-8 w-8 text-primary" />
                  <CardTitle className="text-lg">{title}</CardTitle>
                  <p className="text-xs text-muted-foreground">{titleEn}</p>
                  <Badge variant="outline">{count} mục</Badge>
                  <CardDescription>{desc}</CardDescription>
                </CardHeader>
              </Card>
            </Link>
          );
        })}
      </div>
    </div>
  );
}
```

Key details:
- Server Component (NO `"use client"`)
- Exact Vietnamese strings from UI-SPEC Copywriting Contract
- `max-w-3xl mx-auto` for comfortable reading width
- Responsive grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
- Each card shows icon (32×32, `h-8 w-8 text-primary`), Vietnamese title, English subtitle, count badge, description
- `focus-visible:ring-2` for keyboard navigation accessibility
- `h-full` on Card ensures equal heights in grid

**File 2: `apps/helios/src/app/learn/[category]/page.tsx`** (Server Component shell)

Create directory `apps/helios/src/app/learn/[category]/` first.

```typescript
import { notFound } from "next/navigation";
import { TrendingUp, Calculator, Globe } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { getEntriesByCategory, type GlossaryCategory } from "@/lib/glossary";
import { GlossarySearch } from "@/components/learn/glossary-search";

const VALID_CATEGORIES: GlossaryCategory[] = ["technical", "fundamental", "macro"];

const CATEGORY_META: Record<GlossaryCategory, { title: string; desc: string; icon: typeof TrendingUp }> = {
  technical: { title: "Chỉ Báo Kỹ Thuật", desc: "Các chỉ báo phân tích xu hướng và động lượng giá cổ phiếu", icon: TrendingUp },
  fundamental: { title: "Tỷ Số Cơ Bản", desc: "Các chỉ số đánh giá sức khỏe tài chính và định giá doanh nghiệp", icon: Calculator },
  macro: { title: "Yếu Tố Vĩ Mô", desc: "Các chỉ số kinh tế vĩ mô ảnh hưởng đến thị trường chứng khoán", icon: Globe },
};

// Static generation: build all 3 category pages at build time
export function generateStaticParams() {
  return VALID_CATEGORIES.map((category) => ({ category }));
}

export default async function LearnCategoryPage({
  params,
}: {
  params: Promise<{ category: string }>;
}) {
  const { category } = await params;

  if (!VALID_CATEGORIES.includes(category as GlossaryCategory)) {
    notFound();
  }

  const validCategory = category as GlossaryCategory;
  const entries = getEntriesByCategory(validCategory);
  const meta = CATEGORY_META[validCategory];
  const Icon = meta.icon;

  return (
    <div className="max-w-3xl mx-auto">
      <div className="flex items-center gap-3 mb-1">
        <Icon className="h-6 w-6 text-primary" />
        <h1 className="text-lg font-semibold">{meta.title}</h1>
      </div>
      <div className="flex items-center gap-2 mb-6">
        <Badge variant="outline">{entries.length} mục</Badge>
        <span className="text-sm text-muted-foreground">{meta.desc}</span>
      </div>
      <GlossarySearch entries={entries} category={validCategory} />
    </div>
  );
}
```

Key details:
- Server Component (NO `"use client"`)
- `params` is `Promise<{ category: string }>` — MUST `await params` (Next.js 16 breaking change)
- `generateStaticParams` returns 3 slugs for static generation at build time
- `notFound()` for invalid category slugs (LEARN-03)
- Vietnamese strings from UI-SPEC Copywriting Contract
- Delegates interactivity to `GlossarySearch` client component
- `max-w-3xl mx-auto` for consistent reading width

**File 3: Append to `apps/helios/src/app/globals.css`**

Add collapsible CSS transition rules at the END of the file (after all existing rules):

```css
/* Collapsible height animation for @base-ui/react */
[data-slot="collapsible-content"] {
  overflow: hidden;
  transition: height 200ms ease-out;
}

[data-slot="collapsible-content"][data-closed] {
  height: 0;
}
```

These rules enable smooth expand/collapse animation for the `CollapsibleContent` component. Without them, the content appears/disappears instantly with no transition.
  </action>
  <verify>
    <automated>cd apps/helios && test -f src/app/learn/page.tsx && test -f "src/app/learn/[category]/page.tsx" && grep -q "Học — Kiến Thức Đầu Tư" src/app/learn/page.tsx && grep -q "generateStaticParams" "src/app/learn/[category]/page.tsx" && grep -q "notFound" "src/app/learn/[category]/page.tsx" && grep -q "await params" "src/app/learn/[category]/page.tsx" && grep -q "collapsible-content" src/app/globals.css && npm run build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/helios/src/app/learn/page.tsx` exists
    - Contains "Học — Kiến Thức Đầu Tư" title
    - Contains "Tìm hiểu các chỉ báo kỹ thuật, tỷ số cơ bản và yếu tố vĩ mô được sử dụng trong phân tích AI" subtitle
    - Contains `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` responsive grid
    - Contains `getEntriesByCategory` import from `@/lib/glossary`
    - Does NOT contain `"use client"` (must be Server Component)
    - File `apps/helios/src/app/learn/[category]/page.tsx` exists
    - Contains `export function generateStaticParams` returning 3 category slugs
    - Contains `await params` (Next.js 16 async params)
    - Contains `notFound()` import from `next/navigation`
    - Contains `GlossarySearch` import from `@/components/learn/glossary-search`
    - Does NOT contain `"use client"` (must be Server Component)
    - `apps/helios/src/app/globals.css` contains `[data-slot="collapsible-content"]` with `transition: height 200ms ease-out`
    - `apps/helios/src/app/globals.css` contains `[data-slot="collapsible-content"][data-closed]` with `height: 0`
    - `cd apps/helios && npm run build` passes
  </acceptance_criteria>
  <done>Learn hub page shows 3 category cards with entry counts, category pages render with search + entries, invalid slugs 404, collapsible animation works. All pages are Server Components with client-side interactivity delegated to GlossarySearch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of learn pages</name>
  <files>none — verification only</files>
  <action>
What was built: Complete learn section — hub page (/learn) with 3 category cards, 3 category pages (/learn/technical, /learn/fundamental, /learn/macro) with search and expandable glossary entries, sidebar "Học" navigation.

How to verify:
1. Start dev server: `cd apps/helios && npm run dev`
2. Open http://localhost:3000 — verify sidebar shows "Học" with BookOpen icon
3. Click "Học" — verify `/learn` hub page shows:
   - Title "Học — Kiến Thức Đầu Tư"
   - 3 category cards with icons, Vietnamese titles, entry count badges
   - Responsive: resize browser to verify 1-column on mobile, 3-column on desktop
4. Click "Chỉ Báo Kỹ Thuật" card — verify `/learn/technical` shows:
   - Category title with TrendingUp icon
   - Entry count badge
   - Search input with placeholder "Tìm chỉ báo kỹ thuật..."
   - ≥10 glossary entry cards
5. Click any entry card — verify it expands smoothly with:
   - Formula block (if applicable) in monospace
   - Full markdown article with proper headings, paragraphs
   - Click again to collapse
6. Test search: type "chi so" (without diacritics) — verify it finds entries containing "chỉ số"
7. Test search: type "RSI" — verify RSI entry appears
8. Clear search with × button or Escape key
9. Visit `/learn/invalid` — verify 404 page appears
10. Visit `/learn/fundamental` and `/learn/macro` — verify entries render correctly
11. Check dark mode toggle — verify all learn pages look correct in dark theme

Resume: Type "approved" or describe any visual/functional issues.
  </action>
  <verify>
    <automated>cd apps/helios && npm run build</automated>
  </verify>
  <done>User confirms learn pages render correctly with proper layout, search, expand/collapse, and theme support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client search input → filter logic | User text input used only for client-side `.filter()` string comparison — never sent to server |
| Glossary content → react-markdown | Hardcoded TypeScript strings rendered via react-markdown — no user-generated content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Search input | accept | Input is client-side only, used for `.filter()` string comparison. Never inserted as HTML, never sent to server. No XSS or injection vector. |
| T-09-02 | Tampering | Glossary markdown content | accept | Content is author-controlled hardcoded TypeScript strings. `react-markdown` sanitizes HTML by default (no `dangerouslySetInnerHTML`). |
</threat_model>

<verification>
1. `cd apps/helios && npm run build` — full build passes with all new pages and components
2. Hub page at `/learn` renders 3 category cards with correct Vietnamese titles and entry counts
3. Category pages at `/learn/technical`, `/learn/fundamental`, `/learn/macro` each render their entries
4. `/learn/invalid` returns 404
5. Search with "chi so" matches entries containing "chỉ số" (diacritic-insensitive)
6. Entry cards expand/collapse with smooth animation
7. Expanded entries show formatted markdown articles (headings, paragraphs, code blocks)
8. Both light and dark themes render correctly
</verification>

<success_criteria>
- All 3 category pages accessible via `/learn/[category]` URLs
- Hub page shows 3 clickable category cards with icons, titles, counts
- Search filters entries in real-time with Vietnamese diacritic-insensitive matching
- Entry cards expand to show full markdown articles with proper typography
- Invalid category URLs show 404
- Collapsible animation is smooth (not instant appear/disappear)
- Both themes look correct
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-academic-learning-page-glossary-data/09-02-SUMMARY.md`
</output>
