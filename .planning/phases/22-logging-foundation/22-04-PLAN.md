---
phase: 22-logging-foundation
plan: 04
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - apps/prometheus/src/localstock/services/pipeline.py
  - apps/prometheus/src/localstock/scheduler/scheduler.py
autonomous: true
requirements: [OBS-03]

must_haves:
  truths:
    - "Every log record emitted from inside Pipeline.run_full() body carries extra.run_id == str(pipeline_run.id)"
    - "run_id is the persisted PipelineRun.id (NOT a freshly minted uuid4) — D-03 reuse mandate"
    - "Pipeline contextualize wrap starts AFTER session.commit() (so run.id is populated, A5 risk mitigated)"
    - "run_id_var.set is called alongside logger.contextualize so stdlib-bridged logs (SQLAlchemy/asyncpg) inside the run also gain run_id via the redaction patcher's defensive enrichment"
    - "scheduler daily_job uses logger.exception (not logger.error f-string) inside its except block"
    - "scheduler get_lifespan calls configure_logging() defensively (idempotent guard makes this safe)"
    - "scheduler get_lifespan calls logger.complete() on shutdown to drain enqueued records"
  artifacts:
    - path: "apps/prometheus/src/localstock/services/pipeline.py"
      provides: "run_id-bound run_full body"
      contains: "logger.contextualize(run_id="
    - path: "apps/prometheus/src/localstock/scheduler/scheduler.py"
      provides: "configure_logging() in lifespan + structured daily_job events + logger.complete on shutdown"
  key_links:
    - from: "Pipeline.run_full"
      to: "run_id_var (contextvar) + logger.contextualize(run_id=str(run.id))"
      via: "set token after commit, reset in finally"
      pattern: "run_id_var.set"
    - from: "scheduler get_lifespan"
      to: "observability.configure_logging"
      via: "idempotent defensive call"
      pattern: "configure_logging()"
---

<objective>
Bind run_id (= PipelineRun.id) onto every log record emitted by a pipeline run.
Wire scheduler lifespan to call configure_logging defensively, and convert scheduler
f-string log calls to structured form.

Purpose: Implement OBS-03. Even nested SQLAlchemy/asyncpg/httpx logs inherit run_id
via the contextvar bridge in the redaction patcher.
Output: Edits to 2 existing files (no new modules).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-logging-foundation/22-CONTEXT.md
@.planning/phases/22-logging-foundation/22-RESEARCH.md
@.planning/phases/22-logging-foundation/22-PATTERNS.md
@apps/prometheus/src/localstock/scheduler/scheduler.py
@apps/prometheus/src/localstock/services/pipeline.py
@.planning/phases/22-logging-foundation/22-01-SUMMARY.md

<interfaces>
Consumed from 22-01:

```python
from localstock.observability.logging import configure_logging
from localstock.observability.context import run_id_var
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: services/pipeline.py — wrap run_full body in run_id contextualize</name>
  <files>apps/prometheus/src/localstock/services/pipeline.py</files>
  <read_first>
    - apps/prometheus/src/localstock/services/pipeline.py (read full run_full body, ~lines 50-120)
    - .planning/phases/22-logging-foundation/22-RESEARCH.md section "Pipeline run_id propagation" lines 539-585
    - .planning/phases/22-logging-foundation/22-PATTERNS.md section pipeline.py lines 195-237
    - .planning/phases/22-logging-foundation/22-CONTEXT.md D-03 (reuse PipelineRun.id verbatim — do NOT mint new uuid4)
    - .planning/research/PITFALLS.md Pitfall 7 (contextualize must wrap inside try/except)
  </read_first>
  <behavior>
    - When Pipeline(session).run_full() executes, every loguru record emitted between
      the post-commit point and function return contains extra.run_id == str(run.id)
    - run.id is the persisted PipelineRun primary key UUID, NOT a separate uuid4
    - After run_full returns or raises, run_id_var.get() returns None (token reset)
    - Existing pipeline business logic (Steps 1..N) is UNCHANGED — only the wrapper changes
    - F-string log calls inside run_full converted to structured form
    - "pipeline.run.started" and "pipeline.run.completed" boundary events emitted
  </behavior>
  <action>
    Edit apps/prometheus/src/localstock/services/pipeline.py:

    Step 1. Add import near existing imports (loguru already imported at line 17):

        from localstock.observability.context import run_id_var

    Step 2. Locate "async def run_full(self, run_type: str = 'daily') -> PipelineRun:".
    Find the "await self.session.commit()" line (~line 65). The existing code AFTER that
    commit (the body of run_full from the line following the commit through the end of
    the method, including all `await crawl(...)`, `await analyze(...)`, etc.) MUST be
    moved verbatim INSIDE a new `with logger.contextualize(run_id=str(pipeline_run.id), pipeline_run_id=pipeline_run.id):`
    block. The contextualize block WRAPS the existing body — it does NOT replace it.
    Do NOT replace the body with `pass`. Every existing await/business-logic line is
    preserved unchanged.

    Final structure (showing the wrapper only — existing body stays as-is between the
    markers):

        await self.session.commit()                          # run.id now populated
        run_id = str(run.id)
        token = run_id_var.set(run_id)
        try:
            with logger.contextualize(run_id=run_id):
                logger.info("pipeline.run.started", run_type=run_type)
                try:
                    # >>> EXISTING BODY GOES HERE — every line from after the commit
                    # >>> through the original return statement, copied verbatim.
                    # >>> Apply f-string-to-kwargs rewrites in place (see step 3) but
                    # >>> do NOT delete or reorder business logic.
                finally:
                    logger.info("pipeline.run.completed", status=run.status)
            return run
        finally:
            run_id_var.reset(token)

    Critical: existing return paths (success and exception) end up inside the inner try.
    Preserve every existing await and business-logic line. Only the wrapper structure changes.

    Step 3. Convert ALL f-string log calls in this file to structured form per
    RESEARCH "F-string migration examples" lines 612-619. Example:

        # before:
        logger.info(f"Step 1: Stored {count} HOSE stock listings")
        # after:
        logger.info("pipeline.listings.stored", step=1, count=count)

    Inside except blocks, prefer logger.exception("event.name") to capture stack.
    (Plan 22-05 will sweep the remaining 32 files; this plan handles only pipeline.py
    so that the run_id propagation test cleanly passes without f-string interference.)

    Step 4 (defensive, only if needed). If grep shows PipelineRun uses a server-side
    default for id (e.g. server_default=...), add before "run_id = str(run.id)":

        if run.id is None:
            await self.session.refresh(run)

    Skip this step if id has a Python-side default (default=uuid.uuid4).
  </action>
  <verify>
    <automated>cd apps/prometheus &amp;&amp; uv run pytest tests/test_observability/test_run_id.py -x</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "run_id_var.set" apps/prometheus/src/localstock/services/pipeline.py succeeds
    - grep -q "logger.contextualize(run_id=" apps/prometheus/src/localstock/services/pipeline.py succeeds
    - grep -q "pipeline.run.started" apps/prometheus/src/localstock/services/pipeline.py succeeds
    - grep -q "pipeline.run.completed" apps/prometheus/src/localstock/services/pipeline.py succeeds
    - F-string log lint clean for THIS file: bash -c '! grep -rEn "logger\.(debug|info|warning|error|critical|exception|trace|success)\(\s*f[\"\x27]" apps/prometheus/src/localstock/services/pipeline.py'
    - test_run_id.py is GREEN: cd apps/prometheus && uv run pytest tests/test_observability/test_run_id.py -x exits 0
    - Existing pipeline tests (if any) still pass: cd apps/prometheus && uv run pytest tests/ -x -k "pipeline" exits 0
    - Body preservation check: `git diff apps/prometheus/src/localstock/services/pipeline.py` shows existing pipeline steps preserved (no deletions of `await crawl(...)`, `await analyze(...)`, or any other business-logic call previously present). The diff should consist of additions (wrapper lines, f-string conversions) plus indentation changes only.
  </acceptance_criteria>
  <done>OBS-03 verifiable: run_id present in every log record emitted from run_full body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scheduler/scheduler.py — configure_logging() in lifespan + structured daily_job</name>
  <files>apps/prometheus/src/localstock/scheduler/scheduler.py</files>
  <read_first>
    - apps/prometheus/src/localstock/scheduler/scheduler.py (full file — 90 lines)
    - .planning/phases/22-logging-foundation/22-PATTERNS.md section scheduler.py lines 147-191
    - .planning/phases/22-logging-foundation/22-RESEARCH.md section pipeline run_id propagation lines 575-585 (daily_job example)
    - .planning/phases/22-logging-foundation/22-RESEARCH.md "Open Question 3" lines 767-770 (logger.complete on shutdown)
  </read_first>
  <behavior>
    - get_lifespan calls configure_logging() as the FIRST step inside the async context manager
    - On shutdown (post-yield), logger.complete() is called to drain enqueued records
      before the process exits
    - daily_job uses structured event names: "scheduler.daily.start", "scheduler.daily.result", "scheduler.daily.failed"
    - daily_job's except block uses logger.exception (captures stack) — not logger.error f-string
    - All 3 existing f-string log calls in scheduler.py are eliminated
  </behavior>
  <action>
    Edit apps/prometheus/src/localstock/scheduler/scheduler.py:

    Step 1. Modify daily_job (currently lines ~33-41). Replace the body verbatim with:

        async def daily_job():
            """Scheduled daily pipeline execution."""
            logger.info("scheduler.daily.start")
            service = AutomationService()
            try:
                result = await service.run_daily_pipeline()
                logger.info("scheduler.daily.result", status=result["status"])
            except Exception:
                logger.exception("scheduler.daily.failed")

    Step 2. Replace the f-string log at the bottom of setup_scheduler (currently lines 67-70):

        # before:
        logger.info(
            f"Scheduler configured: daily pipeline at "
            f"{settings.scheduler_run_hour}:{settings.scheduler_run_minute:02d} VN time"
        )
        # after:
        logger.info(
            "scheduler.configured",
            hour=settings.scheduler_run_hour,
            minute=settings.scheduler_run_minute,
            tz="Asia/Ho_Chi_Minh",
        )

    Step 3. Modify get_lifespan (currently ~lines 73-90):

        @asynccontextmanager
        async def get_lifespan(app: FastAPI):
            from localstock import configure_ssl, configure_vnstock_api_key
            from localstock.observability.logging import configure_logging

            configure_logging()  # idempotent; defensive — covers CLI / scheduler-only entry points
            configure_ssl()
            configure_vnstock_api_key()
            setup_scheduler()
            scheduler.start()
            logger.info("scheduler.started")
            yield
            scheduler.shutdown()
            logger.complete()  # drain enqueued records (RESEARCH Open Question 3)
            logger.info("scheduler.stopped")

    Step 4. Verify ZERO remaining f-string log calls in this file:

        ! grep -E 'logger\.[a-z]+\(\s*f["'"'"']' apps/prometheus/src/localstock/scheduler/scheduler.py
  </action>
  <verify>
    <automated>cd apps/prometheus &amp;&amp; uv run pytest tests/ -x -k "scheduler or lifespan" 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "configure_logging()" apps/prometheus/src/localstock/scheduler/scheduler.py succeeds
    - grep -q "logger.complete()" apps/prometheus/src/localstock/scheduler/scheduler.py succeeds
    - grep -q "scheduler.daily.start" apps/prometheus/src/localstock/scheduler/scheduler.py succeeds
    - grep -q "scheduler.daily.failed" apps/prometheus/src/localstock/scheduler/scheduler.py succeeds
    - grep -q "logger.exception" apps/prometheus/src/localstock/scheduler/scheduler.py succeeds
    - F-string lint clean for this file: bash -c '! grep -E "logger\.(debug|info|warning|error|critical|exception|trace|success)\(\s*f[\"\x27]" apps/prometheus/src/localstock/scheduler/scheduler.py'
    - App still imports cleanly: cd apps/prometheus && uv run python -c "from localstock.api.app import create_app; create_app(); print('OK')" prints OK
  </acceptance_criteria>
  <done>Scheduler defensive configure_logging in place; daily_job structured; ready for Wave 3 file sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline orchestrator → log sink | Pipeline emits operational metadata (counts, statuses); no untrusted external input crosses |
| scheduler → log sink | Cron-triggered execution; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-08 | Repudiation | scheduler daily_job swallowing exceptions via logger.error(f"...{e}") | mitigate | Replace with logger.exception("scheduler.daily.failed") which captures full stack trace via loguru's exc_info handling |
| T-22-09 | Information Disclosure | run_id appears in logs | accept | run_id is a UUID (not a secret); inclusion is the entire point of OBS-03 |
| T-22-10 | DoS | logger.complete() on slow shutdown | accept | Drain is bounded by enqueue queue size; uvicorn graceful shutdown timeout already enforces an upper bound |
</threat_model>

<verification>
- uv run pytest tests/test_observability/test_run_id.py -x exits 0
- uv run pytest tests/ -x (full suite) — no regression
- Manual smoke: trigger /api/automation/run, observe stdout log lines, confirm every record carries run_id
</verification>

<success_criteria>
- OBS-03: test_run_id.py asserts every record emitted within run_full body has extra.run_id == str(run.id)
- Scheduler f-string-free; logger.exception captures stack on failure
- Lifespan startup calls configure_logging defensively (idempotent — Pitfall 5 unaffected)
</success_criteria>

<output>
After completion, create .planning/phases/22-logging-foundation/22-04-SUMMARY.md
</output>
