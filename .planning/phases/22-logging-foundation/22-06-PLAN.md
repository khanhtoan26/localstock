---
phase: 22-logging-foundation
plan: 06
type: execute
wave: 4
depends_on: [22-05]
files_modified:
  - .pre-commit-config.yaml
  - .github/workflows/lint.yml
autonomous: true
requirements: [OBS-06]

must_haves:
  truths:
    - ".pre-commit-config.yaml exists at repo root with a 'no-fstring-log' hook that invokes apps/prometheus/scripts/lint-no-fstring-logs.sh"
    - "Pre-commit hook fails with non-zero exit when a developer reintroduces a f-string log call"
    - "GitHub Actions workflow .github/workflows/lint.yml runs the same lint script on every PR + push"
    - "Both gates succeed RIGHT NOW (because Wave 3 cleaned the sweep) — pre-commit run --all-files exits 0"
    - "A synthetic violation (temporary git stash demo) makes both gates fail correctly — proves the gate has teeth"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "Local commit-time gate"
      contains: "no-fstring-log"
    - path: ".github/workflows/lint.yml"
      provides: "PR / push CI gate"
      contains: "lint-no-fstring-logs.sh"
  key_links:
    - from: ".pre-commit-config.yaml hook"
      to: "apps/prometheus/scripts/lint-no-fstring-logs.sh"
      via: "entry: bash apps/prometheus/scripts/lint-no-fstring-logs.sh"
      pattern: "lint-no-fstring-logs.sh"
    - from: ".github/workflows/lint.yml step"
      to: "apps/prometheus/scripts/lint-no-fstring-logs.sh"
      via: "run: bash apps/prometheus/scripts/lint-no-fstring-logs.sh"
      pattern: "lint-no-fstring-logs.sh"
---

<objective>
Make OBS-06 self-enforcing: the lint script created in 22-00 and made green by 22-05
must now be wired into both pre-commit (local fast-fail) and GitHub Actions (PR gate)
so future regressions are auto-rejected.

Per CONTEXT.md D-07, both layers are mandatory. RESEARCH §"Open Question A6" notes
.github/workflows/ does not yet exist — this plan creates it minimally
(lint-only workflow; broader CI is a separate milestone concern).

Purpose: OBS-06 is verifiable in CI on every PR; future contributors cannot accidentally
reintroduce f-string log violations.
Output: Two new YAML files at repo root.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-logging-foundation/22-CONTEXT.md
@.planning/phases/22-logging-foundation/22-RESEARCH.md
@.planning/phases/22-logging-foundation/22-PATTERNS.md
@apps/prometheus/scripts/lint-no-fstring-logs.sh
@.planning/phases/22-logging-foundation/22-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: .pre-commit-config.yaml — local fast-fail hook</name>
  <files>.pre-commit-config.yaml</files>
  <read_first>
    - .planning/phases/22-logging-foundation/22-RESEARCH.md section "Pre-commit hook" lines 621-634
    - .planning/phases/22-logging-foundation/22-CONTEXT.md D-07
    - apps/prometheus/scripts/lint-no-fstring-logs.sh (the script we wire to)
  </read_first>
  <action>
    Create .pre-commit-config.yaml at repo root with one local hook that delegates to
    the existing lint shell script (single source of truth — same script CI uses):

        # .pre-commit-config.yaml
        # Phase 22 (OBS-06): block f-string log calls at commit time.
        # The hook simply invokes the canonical lint script so pre-commit and CI agree.
        repos:
          - repo: local
            hooks:
              - id: no-fstring-log
                name: "loguru: no f-string log calls"
                entry: bash apps/prometheus/scripts/lint-no-fstring-logs.sh
                language: system
                pass_filenames: false
                always_run: true

    Do NOT add other unrelated hooks (ruff, black, mypy) in this phase — keeps the file
    focused on Phase 22 scope. Future phases may extend.
  </action>
  <verify>
    <automated>test -f .pre-commit-config.yaml &amp;&amp; grep -q "no-fstring-log" .pre-commit-config.yaml &amp;&amp; grep -q "lint-no-fstring-logs.sh" .pre-commit-config.yaml</automated>
  </verify>
  <acceptance_criteria>
    - File .pre-commit-config.yaml exists at repo root
    - Contains a `no-fstring-log` hook id: grep -q "id: no-fstring-log" .pre-commit-config.yaml
    - Delegates to the script: grep -q "lint-no-fstring-logs.sh" .pre-commit-config.yaml
    - YAML parses (use python yaml or yq): `python -c "import yaml; yaml.safe_load(open('.pre-commit-config.yaml'))"` exits 0
    - Manual run of the underlying script succeeds (the gate is currently green): `bash apps/prometheus/scripts/lint-no-fstring-logs.sh` exits 0
    - Hook spec uses `pass_filenames: false` and `always_run: true` so partial commits still trigger full repo scan
  </acceptance_criteria>
  <done>Local commit-time gate active; script is single source of truth for both gates.</done>
</task>

<task type="auto">
  <name>Task 2: .github/workflows/lint.yml — minimal CI gate</name>
  <files>.github/workflows/lint.yml</files>
  <read_first>
    - .planning/phases/22-logging-foundation/22-RESEARCH.md section "GitHub Actions step" lines 636-649 + Open Question A6 lines 750-752
    - apps/prometheus/scripts/lint-no-fstring-logs.sh
  </read_first>
  <action>
    Create .github/workflows/lint.yml — minimal lint-only workflow. RESEARCH Open Question A6
    flags that no .github/workflows/ exists; per CONTEXT.md D-07 both layers are required,
    so we create a tightly-scoped lint workflow rather than deferring.

        # .github/workflows/lint.yml
        # Phase 22 (OBS-06): PR / push gate for f-string log calls.
        name: lint

        on:
          push:
            branches: [main]
          pull_request:
            branches: [main]

        jobs:
          no-fstring-log:
            runs-on: ubuntu-latest
            steps:
              - uses: actions/checkout@v4
              - name: "loguru: no f-string log calls"
                run: bash apps/prometheus/scripts/lint-no-fstring-logs.sh

    Do NOT add other lint steps (ruff, mypy) in this phase — separate concern.
    Action versions pinned to v4 (currently latest stable).
  </action>
  <verify>
    <automated>test -f .github/workflows/lint.yml &amp;&amp; python -c "import yaml; yaml.safe_load(open('.github/workflows/lint.yml'))" &amp;&amp; grep -q "lint-no-fstring-logs.sh" .github/workflows/lint.yml</automated>
  </verify>
  <acceptance_criteria>
    - File .github/workflows/lint.yml exists
    - Valid YAML: python -c "import yaml; yaml.safe_load(open('.github/workflows/lint.yml'))" exits 0
    - References the canonical script: grep -q "apps/prometheus/scripts/lint-no-fstring-logs.sh" .github/workflows/lint.yml
    - Triggers on PR + push to main: grep -q "pull_request:" and grep -q "push:" both succeed
    - Uses actions/checkout@v4 (or newer): grep -qE "actions/checkout@v[4-9]" .github/workflows/lint.yml
    - The underlying script still exits 0 today (the gate is currently green): bash apps/prometheus/scripts/lint-no-fstring-logs.sh exits 0
  </acceptance_criteria>
  <done>OBS-06 enforced on every PR; CONTEXT.md D-07 satisfied (both pre-commit + GHA layers).</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end gate sanity check (synthetic violation)</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - apps/prometheus/scripts/lint-no-fstring-logs.sh
  </read_first>
  <action>
    Prove the gate has teeth: temporarily inject a synthetic f-string log line, confirm
    the script catches it, then revert. This validates that 22-05's clean state is not
    a false negative caused by a bug in the lint script regex.

      1. Pick any existing src file (e.g. apps/prometheus/src/localstock/api/app.py)
      2. Append a single line: `# logger.info(f"sanity {1}")` → NO, comments don't count.
         Instead temporarily INSERT a real-looking line at module bottom:
         `_ = lambda: __import__("loguru").logger.info(f"sanity-check-{1}")`
         This is a callable that pattern-matches `logger.info(f"`.
      3. Run: `bash apps/prometheus/scripts/lint-no-fstring-logs.sh; test $? -ne 0`
         Confirm script exits NON-zero and prints the offending line.
      4. Revert the file: `git checkout -- apps/prometheus/src/localstock/api/app.py`
      5. Run: `bash apps/prometheus/scripts/lint-no-fstring-logs.sh` exits 0 again.

    This is a one-shot smoke test for the gate; do NOT commit the synthetic violation.
  </action>
  <verify>
    <automated>bash apps/prometheus/scripts/lint-no-fstring-logs.sh &amp;&amp; git diff --quiet apps/prometheus/src/</automated>
  </verify>
  <acceptance_criteria>
    - Synthetic violation was detected (script exited non-zero) — recorded in SUMMARY.md
    - File reverted: git diff --quiet apps/prometheus/src/ exits 0 (no leftover modifications)
    - Final lint check passes: bash apps/prometheus/scripts/lint-no-fstring-logs.sh exits 0
    - Clean working tree: git status -s reports zero changes in src/
  </acceptance_criteria>
  <done>Phase 22 verified end-to-end; OBS-06 gate proven both green-on-clean and red-on-dirty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer commit → repo | Pre-commit hook scans staged source for regressions before they reach origin |
| PR / push → main | GHA workflow scans repo state on every PR; merge-block on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-13 | Tampering | regression of OBS-06 (someone reintroduces f-string log) | mitigate | Pre-commit hook + GHA workflow both invoke the same canonical script; bypassing requires explicit `# noqa: log-fstring` token in the source line |
| T-22-14 | Repudiation | hook silently skipped (developer skips pre-commit with --no-verify) | accept | GHA workflow is the authoritative gate; --no-verify only bypasses the local convenience layer, not the merge gate |
</threat_model>

<verification>
- bash apps/prometheus/scripts/lint-no-fstring-logs.sh exits 0 (green-on-clean)
- python yaml.safe_load on both YAML files succeeds
- Manual smoke (Task 3) confirms the gate detects synthetic violations
</verification>

<success_criteria>
- OBS-06 wired into pre-commit AND GitHub Actions per D-07
- Both gates currently GREEN (Wave 3 sweep + this plan close the loop)
- Synthetic violation test (Task 3) proves gate has teeth
</success_criteria>

<output>
After completion, create .planning/phases/22-logging-foundation/22-06-SUMMARY.md
</output>
