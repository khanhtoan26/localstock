---
phase: 12.1-performance-polish
plan: "01"
subsystem: admin-console
tags: [cache-invalidation, job-transitions, i18n, css-animation]

dependency_graph:
  requires: []
  provides:
    - invalidateForJob utility function
    - getJobSymbols utility function
    - useJobTransitions hook
    - job-row-highlight CSS animation
    - Toast i18n keys (EN + VI)
  affects:
    - apps/helios/src/lib/queries.ts
    - apps/helios/src/hooks/use-job-transitions.ts
    - apps/helios/src/app/globals.css
    - apps/helios/messages/en.json
    - apps/helios/messages/vi.json

tech_stack:
  added: []
  patterns:
    - "ref-based status diffing for polling transition detection"
    - "targeted query invalidation by job type → query key mapping"
    - "color-mix CSS keyframe animation for row highlights"

key_files:
  created:
    - apps/helios/src/hooks/use-job-transitions.ts
  modified:
    - apps/helios/src/lib/queries.ts
    - apps/helios/src/app/globals.css
    - apps/helios/messages/en.json
    - apps/helios/messages/vi.json

decisions:
  - "crawl also invalidates indicators (depends on refreshed price data)"
  - "isInitialLoadRef skips first data arrival to prevent toast flood on page load"
  - "Transitions collected into array before firing callbacks for batch safety"

metrics:
  duration: "2m 2s"
  completed: "2026-04-23T04:59:14Z"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 5
---

# Phase 12.1 Plan 01: Cache Invalidation Foundation & Job Transition Hook Summary

Targeted query invalidation utility mapping 5 job types to their exact query keys, plus useJobTransitions hook with initial-load skip for detecting running→completed/failed transitions via ref-based diffing.

## Tasks Completed

| # | Task | Commit | Key Changes |
|---|------|--------|-------------|
| 1 | Add invalidateForJob utility + i18n toast keys | `f31cd02` | getJobSymbols + invalidateForJob in queries.ts, 5 toast keys in EN/VI |
| 2 | Create useJobTransitions hook + CSS keyframe animation | `3af077b` | use-job-transitions.ts hook, .job-row-highlight CSS animation |

## Implementation Details

### invalidateForJob (queries.ts)

Pure exported function (not a hook) taking `QueryClient` + `AdminJob`. Switch-based mapping:

- **crawl** → `["prices", s]`, `["indicators", s]`, `["admin", "stocks"]`
- **analyze** → `["technical", s]`, `["fundamental", s]`
- **score** → `["scores", s]`, `["scores", "top"]`
- **report** → `["report", s]`
- **pipeline** → all 6 per-symbol keys + `["scores", "top"]` + `["admin", "stocks"]`

### useJobTransitions (use-job-transitions.ts)

Calls `useAdminJobs()` and diffs via `useRef<Map<number, string>>`. Key behaviors:
- `isInitialLoadRef` starts `true`, set to `false` after first effect → prevents false-positive toasts
- Transitions collected into array before firing callbacks → handles batch completions
- `onTransition` in useEffect deps → caller must wrap in `useCallback`

### CSS Animation (globals.css)

`@keyframes job-highlight`: 300ms hold at `color-mix(in srgb, var(--primary) 8%, transparent)`, then 2000ms fade to transparent. Total 2300ms with `ease-out forwards`.

### i18n Keys Added

5 keys in both EN and VI under `admin.toast`:
- `jobCompleted`, `jobFailed` (with `{type}` + `{symbols}` params)
- `jobCompletedGeneric`, `jobFailedGeneric` (with `{type}` only)
- `viewJob` ("View job" / "Xem tác vụ")

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check: PASSED
