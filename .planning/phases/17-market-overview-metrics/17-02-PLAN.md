---
phase: 17-market-overview-metrics
plan: "02"
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - apps/helios/src/lib/types.ts
  - apps/helios/src/lib/queries.ts
  - apps/helios/src/components/market/market-summary-cards.tsx
  - apps/helios/src/app/market/page.tsx
  - apps/helios/messages/en.json
  - apps/helios/messages/vi.json
autonomous: true
requirements:
  - MKT-03

must_haves:
  truths:
    - "Market Overview page shows 4 metric cards: VN-Index, Total Volume, Advances, Market Breadth"
    - "Each card shows primary value + % change + trend arrow (green ↑ positive, red ↓ negative) per D-05"
    - "Market Summary section is the FIRST section on the market page, before MacroCards per D-08"
    - "Loading state shows 4 skeleton cards; error state shows ErrorState component per D-11"
    - "Small 'as of [date]' label appears inline with section heading when data is loaded per D-04"
    - "Both en.json and vi.json have all required market.summary* keys"
    - "TypeScript build succeeds with no type errors"
  artifacts:
    - path: "apps/helios/src/lib/types.ts"
      provides: "MarketSummaryResponse and VnindexData TypeScript interfaces"
      contains: "MarketSummaryResponse"
      exports: ["MarketSummaryResponse", "VnindexData"]
    - path: "apps/helios/src/lib/queries.ts"
      provides: "useMarketSummary TanStack Query hook"
      contains: "useMarketSummary"
    - path: "apps/helios/src/components/market/market-summary-cards.tsx"
      provides: "4-card grid component with skeleton, change %, trend arrows"
      contains: "MarketSummaryCards"
    - path: "apps/helios/src/app/market/page.tsx"
      provides: "Market page with summary section at top"
      contains: "useMarketSummary"
    - path: "apps/helios/messages/en.json"
      provides: "English i18n keys for market summary section"
      contains: "summaryTitle"
    - path: "apps/helios/messages/vi.json"
      provides: "Vietnamese i18n keys for market summary section"
      contains: "summaryTitle"
  key_links:
    - from: "apps/helios/src/app/market/page.tsx"
      to: "apps/helios/src/lib/queries.ts"
      via: "useMarketSummary() hook import"
      pattern: "useMarketSummary"
    - from: "apps/helios/src/app/market/page.tsx"
      to: "apps/helios/src/components/market/market-summary-cards.tsx"
      via: "MarketSummaryCards component render"
      pattern: "MarketSummaryCards"
    - from: "apps/helios/src/lib/queries.ts"
      to: "/api/market/summary"
      via: "apiFetch<MarketSummaryResponse>"
      pattern: "apiFetch.*market/summary"
---

<objective>
Build the complete frontend for Phase 17: TypeScript types, TanStack Query hook, the MarketSummaryCards component, market page integration, and both locale files. This satisfies MKT-03 — 4 metric cards on the Market Overview page backed by real API data.

Purpose: After Wave 1 ships the backend, the frontend needs types, a data hook, a display component, page integration, and i18n strings. All 6 files must be consistent with each other — missing any one breaks TypeScript compilation.
Output: All 6 frontend files updated and verified with `npm run build`.
</objective>

<execution_context>
@/home/toanak/workspace/localstock/.claude/get-shit-done/workflows/execute-plan.md
@/home/toanak/workspace/localstock/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/toanak/workspace/localstock/.planning/PROJECT.md
@/home/toanak/workspace/localstock/.planning/ROADMAP.md
@/home/toanak/workspace/localstock/.planning/phases/17-market-overview-metrics/17-01-SUMMARY.md

<interfaces>
<!-- From apps/helios/src/lib/types.ts — existing interface pattern to follow -->
```typescript
export interface MacroIndicator {
  indicator_type: string;
  value: number;
  period: string;
  source: string;
  trend: string | null;
  recorded_at: string;
}

export interface MacroLatestResponse {
  indicators: MacroIndicator[];
  count: number;
}

export interface SectorsLatestResponse {
  date: string | null;
  count: number;
  sectors: SectorPerformance[];
}
// New interfaces to add after SectorsLatestResponse, before StockReport:
```

<!-- From apps/helios/src/lib/queries.ts — useMacroLatest hook to mirror exactly -->
```typescript
export function useMacroLatest() {
  return useQuery({
    queryKey: ["macro", "latest"],
    queryFn: () => apiFetch<MacroLatestResponse>(`/api/macro/latest`),
    staleTime: 60 * 60 * 1000,
  });
}
// useMarketSummary goes AFTER useSectorsLatest (line 97), BEFORE useTriggerPipeline
```

<!-- From apps/helios/src/components/market/macro-cards.tsx — component structure to follow -->
```typescript
"use client";
import { useTranslations } from "next-intl";
import { Card, CardContent } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";
// Props interface:
// { indicators: MacroIndicator[]; isLoading?: boolean }
// Skeleton pattern: 2 Skeleton elements per card (h-3 w-20 + h-6 w-24)
// Card: <Card className="border border-border"><CardContent className="p-4">
// Label: <p className="text-xs text-muted-foreground">
// Value: <p className="text-xl font-semibold font-mono text-foreground mt-1">
// Grid: <div className="grid grid-cols-2 gap-4">
```

<!-- From apps/helios/src/app/market/page.tsx — current structure -->
```typescript
"use client";
import { useMacroLatest, useSectorsLatest } from "@/lib/queries";
// Current: macro section is FIRST section after <h1>
// New: market summary section inserts BEFORE macro section (per D-08)

// ErrorState usage pattern:
// {macro.isError ? <ErrorState body={t("macroError")} /> : <MacroCards ... />}
```

<!-- i18n namespace: market.* in both locale files -->
<!-- Current market keys: title, macroTitle, sectorTitle, macroError, sectorError, emptyBody, macroLabels.*, sectorColumns.* -->
<!-- New keys required (per D-04, D-06, D-11): summaryTitle, summaryError, asOf, summaryLabels.vnindex, summaryLabels.totalVolume, summaryLabels.advances, summaryLabels.breadth -->

<!-- next-intl: useTranslations("market") — t("summaryTitle"), t("asOf", { date: "2026-04-25" }) -->
<!-- CRITICAL: both en.json and vi.json MUST be updated in same commit — next-intl throws on missing keys -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TypeScript types and useMarketSummary hook</name>
  <files>
    apps/helios/src/lib/types.ts
    apps/helios/src/lib/queries.ts
  </files>
  <read_first>
    - apps/helios/src/lib/types.ts (full file — find SectorsLatestResponse location to insert after; understand existing interface pattern)
    - apps/helios/src/lib/queries.ts (full file — find useSectorsLatest location to insert after; confirm import block at top)
  </read_first>
  <action>
MODIFY `apps/helios/src/lib/types.ts`:

Insert these two new interfaces AFTER `SectorsLatestResponse` (which ends around line 87) and BEFORE `StockReport`:

```typescript
export interface VnindexData {
  value: number | null;
  change_pct: number | null;
}

export interface MarketSummaryResponse {
  vnindex: VnindexData | null;
  total_volume: number | null;
  total_volume_change_pct: number | null;
  advances: number;
  declines: number;
  breadth: number | null;
  as_of: string | null;
}
```

MODIFY `apps/helios/src/lib/queries.ts`:

1. Add `MarketSummaryResponse` to the existing import block at the top (lines 4-19). Find the `import type { ... } from "./types";` block and add `MarketSummaryResponse` to it.

2. Add `useMarketSummary` function AFTER `useSectorsLatest` (currently ending around line 97) and BEFORE `useTriggerPipeline`:

```typescript
export function useMarketSummary() {
  return useQuery({
    queryKey: ["market", "summary"],
    queryFn: () => apiFetch<MarketSummaryResponse>(`/api/market/summary`),
    staleTime: 30 * 60 * 1000, // D-10: 30 minutes — daily crawl data rhythm
  });
}
```

Do NOT modify any existing hooks. Only add the new function and the import.
  </action>
  <verify>
    <automated>cd /home/toanak/workspace/localstock/apps/helios && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "MarketSummaryResponse" apps/helios/src/lib/types.ts` returns a match
    - `grep "VnindexData" apps/helios/src/lib/types.ts` returns a match
    - `grep "useMarketSummary" apps/helios/src/lib/queries.ts` returns a match
    - `grep "MarketSummaryResponse" apps/helios/src/lib/queries.ts` returns a match (in both import and function body)
    - `grep "market.*summary" apps/helios/src/lib/queries.ts` returns a match on the queryKey line
    - `grep "30 \* 60 \* 1000" apps/helios/src/lib/queries.ts` returns a match (staleTime = 30 minutes per D-10)
    - `cd apps/helios && npm run build` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>VnindexData and MarketSummaryResponse interfaces added to types.ts; useMarketSummary hook added to queries.ts with correct queryKey and staleTime; npm run build exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create MarketSummaryCards component, update market page, add i18n keys</name>
  <files>
    apps/helios/src/components/market/market-summary-cards.tsx
    apps/helios/src/app/market/page.tsx
    apps/helios/messages/en.json
    apps/helios/messages/vi.json
  </files>
  <read_first>
    - apps/helios/src/components/market/macro-cards.tsx (full file — exact skeleton pattern, Card/CardContent usage, label/value formatting to replicate)
    - apps/helios/src/app/market/page.tsx (full file — current import list, hook calls, section JSX structure to modify)
    - apps/helios/messages/en.json (lines 56-75 — market.* block to extend; insert new keys after "emptyBody")
    - apps/helios/messages/vi.json (lines 56-75 — market.* block; Vietnamese equivalents)
  </read_first>
  <action>
CREATE `apps/helios/src/components/market/market-summary-cards.tsx`:

```typescript
"use client";
import { useTranslations } from "next-intl";
import { Card, CardContent } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";
import type { MarketSummaryResponse } from "@/lib/types";

interface MarketSummaryCardsProps {
  data: MarketSummaryResponse | undefined;
  isLoading?: boolean;
}

const CARD_KEYS = ["vnindex", "totalVolume", "advances", "breadth"] as const;

export function MarketSummaryCards({ data, isLoading }: MarketSummaryCardsProps) {
  const t = useTranslations("market");

  if (isLoading) {
    return (
      <div className="grid grid-cols-2 gap-4">
        {CARD_KEYS.map((key) => (
          <Card key={key} className="border border-border">
            <CardContent className="p-4">
              <Skeleton className="h-3 w-20 mb-2" />
              <Skeleton className="h-6 w-24" />
              <Skeleton className="h-3 w-16 mt-1" />
            </CardContent>
          </Card>
        ))}
      </div>
    );
  }

  const vnindexValue = data?.vnindex?.value;
  const vnindexChange = data?.vnindex?.change_pct;
  const totalVolume = data?.total_volume;
  const volumeChange = data?.total_volume_change_pct;
  const advances = data?.advances ?? 0;
  const declines = data?.declines ?? 0;
  const breadth = data?.breadth;
  const total = advances + declines;

  function formatChange(pct: number | null | undefined): string {
    if (pct == null) return "—";
    const sign = pct >= 0 ? "+" : "";
    return `${sign}${pct.toFixed(2)}%`;
  }

  function formatVolume(vol: number | null | undefined): string {
    if (vol == null) return "—";
    if (vol >= 1_000_000_000) return `${(vol / 1_000_000_000).toFixed(1)}B`;
    if (vol >= 1_000_000) return `${(vol / 1_000_000).toFixed(1)}M`;
    return new Intl.NumberFormat("vi-VN").format(vol);
  }

  function ChangeRow({ pct }: { pct: number | null | undefined }) {
    if (pct == null) return <p className="text-xs text-muted-foreground mt-1">—</p>;
    const positive = pct >= 0;
    return (
      <p className={`text-xs mt-1 ${positive ? "text-green-600 dark:text-green-500" : "text-red-500 dark:text-red-400"}`}>
        {positive ? "↑" : "↓"} {formatChange(pct)}
      </p>
    );
  }

  return (
    <div className="grid grid-cols-2 gap-4">
      {/* Card 1: VN-Index (D-06) */}
      <Card className="border border-border">
        <CardContent className="p-4">
          <p className="text-xs text-muted-foreground">{t("summaryLabels.vnindex")}</p>
          <p className="text-xl font-semibold font-mono text-foreground mt-1">
            {vnindexValue != null
              ? new Intl.NumberFormat("vi-VN", { maximumFractionDigits: 1 }).format(vnindexValue)
              : "—"}
          </p>
          <ChangeRow pct={vnindexChange} />
        </CardContent>
      </Card>

      {/* Card 2: Total Volume (D-06) */}
      <Card className="border border-border">
        <CardContent className="p-4">
          <p className="text-xs text-muted-foreground">{t("summaryLabels.totalVolume")}</p>
          <p className="text-xl font-semibold font-mono text-foreground mt-1">
            {formatVolume(totalVolume)}
          </p>
          <ChangeRow pct={volumeChange} />
        </CardContent>
      </Card>

      {/* Card 3: Advances (D-06) */}
      <Card className="border border-border">
        <CardContent className="p-4">
          <p className="text-xs text-muted-foreground">{t("summaryLabels.advances")}</p>
          <p className="text-xl font-semibold font-mono text-foreground mt-1">
            {total > 0
              ? `${advances} / ${declines}`
              : "—"}
          </p>
          {total > 0 ? (
            <p className="text-xs text-muted-foreground mt-1">
              {advances} up · {declines} down
            </p>
          ) : (
            <p className="text-xs text-muted-foreground mt-1">—</p>
          )}
        </CardContent>
      </Card>

      {/* Card 4: Market Breadth (D-06) */}
      <Card className="border border-border">
        <CardContent className="p-4">
          <p className="text-xs text-muted-foreground">{t("summaryLabels.breadth")}</p>
          <p className="text-xl font-semibold font-mono text-foreground mt-1">
            {breadth != null ? `${breadth.toFixed(1)}%` : "—"}
          </p>
          {breadth != null ? (
            <p className={`text-xs mt-1 ${breadth >= 50 ? "text-green-600 dark:text-green-500" : "text-red-500 dark:text-red-400"}`}>
              {breadth >= 50 ? "↑" : "↓"} {breadth >= 50 ? "Bullish" : "Bearish"}
            </p>
          ) : (
            <p className="text-xs text-muted-foreground mt-1">—</p>
          )}
        </CardContent>
      </Card>
    </div>
  );
}
```

MODIFY `apps/helios/src/app/market/page.tsx`:

Replace the entire file content with:

```typescript
"use client";
import { useMacroLatest, useMarketSummary, useSectorsLatest } from "@/lib/queries";
import { useTranslations } from "next-intl";
import { MacroCards } from "@/components/market/macro-cards";
import { MarketSummaryCards } from "@/components/market/market-summary-cards";
import { SectorTable } from "@/components/market/sector-table";
import { EmptyState } from "@/components/ui/empty-state";
import { ErrorState } from "@/components/ui/error-state";
import { Skeleton } from "@/components/ui/skeleton";

export default function MarketPage() {
  const macro = useMacroLatest();
  const sectors = useSectorsLatest();
  const summary = useMarketSummary();
  const t = useTranslations("market");

  return (
    <div>
      <h1 className="text-xl font-semibold mb-6">{t("title")}</h1>

      {/* Market Summary section — FIRST after page title (D-08) */}
      <section className="mb-8">
        <div className="flex items-baseline justify-between mb-4">
          <h2 className="text-sm font-semibold text-muted-foreground">{t("summaryTitle")}</h2>
          {summary.data?.as_of && (
            <span className="text-xs text-muted-foreground">
              {t("asOf", { date: summary.data.as_of })}
            </span>
          )}
        </div>
        {summary.isError ? (
          <ErrorState body={t("summaryError")} />
        ) : (
          <MarketSummaryCards data={summary.data} isLoading={summary.isLoading} />
        )}
      </section>

      {/* Macro indicator cards */}
      <section className="mb-8">
        <h2 className="text-sm font-semibold text-muted-foreground mb-4">{t("macroTitle")}</h2>
        {macro.isError ? (
          <ErrorState body={t("macroError")} />
        ) : (
          <MacroCards
            indicators={macro.data?.indicators || []}
            isLoading={macro.isLoading}
          />
        )}
      </section>

      {/* Sector performance table */}
      <section>
        <h2 className="text-sm font-semibold text-muted-foreground mb-4">{t("sectorTitle")}</h2>
        {sectors.isLoading ? (
          <div className="space-y-3">
            {Array.from({ length: 6 }).map((_, i) => (
              <Skeleton key={i} className="h-10 w-full" />
            ))}
          </div>
        ) : sectors.isError ? (
          <ErrorState body={t("sectorError")} />
        ) : !sectors.data || sectors.data.count === 0 ? (
          <EmptyState body={t("emptyBody")} />
        ) : (
          <SectorTable sectors={sectors.data.sectors} />
        )}
      </section>
    </div>
  );
}
```

MODIFY `apps/helios/messages/en.json`:

In the `"market"` object, insert these keys AFTER `"emptyBody"` and BEFORE `"macroLabels"`:

```json
"summaryTitle": "Market Summary",
"summaryError": "Unable to load market summary.",
"asOf": "As of {date}",
"summaryLabels": {
  "vnindex": "VN-Index",
  "totalVolume": "Total Volume",
  "advances": "Advances / Declines",
  "breadth": "Market Breadth"
},
```

MODIFY `apps/helios/messages/vi.json`:

In the `"market"` object, insert these keys AFTER `"emptyBody"` and BEFORE `"macroLabels"` (Vietnamese translations):

```json
"summaryTitle": "Tổng quan thị trường",
"summaryError": "Không thể tải dữ liệu tổng quan thị trường.",
"asOf": "Cập nhật: {date}",
"summaryLabels": {
  "vnindex": "VN-Index",
  "totalVolume": "Tổng khối lượng",
  "advances": "Tăng / Giảm",
  "breadth": "Độ rộng thị trường"
},
```

CRITICAL: Both en.json and vi.json MUST be updated — next-intl throws a runtime error if a key exists in one locale but not the other. The `{date}` placeholder in `asOf` is a next-intl ICU message format parameter — it must use curly braces exactly as shown.
  </action>
  <verify>
    <automated>cd /home/toanak/workspace/localstock/apps/helios && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `apps/helios/src/components/market/market-summary-cards.tsx` exists
    - `grep "export function MarketSummaryCards" apps/helios/src/components/market/market-summary-cards.tsx` returns a match
    - `grep "grid grid-cols-2 gap-4" apps/helios/src/components/market/market-summary-cards.tsx` returns a match (D-09: grid layout)
    - `grep "useMarketSummary" apps/helios/src/app/market/page.tsx` returns a match
    - `grep "MarketSummaryCards" apps/helios/src/app/market/page.tsx` returns a match
    - `grep "summaryTitle" apps/helios/src/app/market/page.tsx` returns a match
    - `grep "as_of" apps/helios/src/app/market/page.tsx` returns a match (D-04: "as of" label)
    - `grep "summaryTitle" apps/helios/messages/en.json` returns a match
    - `grep "summaryTitle" apps/helios/messages/vi.json` returns a match
    - `grep "asOf" apps/helios/messages/en.json` returns a match containing `{date}`
    - `grep "asOf" apps/helios/messages/vi.json` returns a match containing `{date}`
    - `cd apps/helios && npm run build` exits 0 — no TypeScript or compilation errors
    - Market Summary section renders BEFORE the Macro section in page.tsx JSX (check comment order)
  </acceptance_criteria>
  <done>market-summary-cards.tsx created with 4 cards, skeleton, change indicators (green ↑ / red ↓); market/page.tsx updated with summary section first; both locale files have all summary* keys; npm run build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js page | Client renders market summary data; all data comes from backend API |
| Next.js → GET /api/market/summary | TanStack Query fetch; staleTime 30 minutes limits request rate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Information Disclosure | MarketSummaryCards | accept | Component renders aggregate market data only (VN-Index, volume, advances/declines). No PII, no user data. All data publicly available. |
| T-17-02-02 | Denial of Service | useMarketSummary | mitigate | staleTime: 30 * 60 * 1000 (30 minutes) per D-10 — browser will not re-fetch more often than once per 30 minutes. Consistent with daily-crawl data rhythm. |
| T-17-02-03 | Tampering | i18n key injection | accept | next-intl keys are static strings in bundled JSON files. No user input is used as i18n keys. XSS via ICU message params: {date} is a backend date string in ISO format (YYYY-MM-DD), not user-controlled. |
| T-17-02-04 | Spoofing | apiFetch response | accept | Response is typed as MarketSummaryResponse via TypeScript generics. If backend returns unexpected shape, TypeScript errors surface at build time. Runtime null guards in the component prevent crashes. |
</threat_model>

<verification>
TypeScript build (required — catches type mismatches between types.ts and component):
```bash
cd /home/toanak/workspace/localstock/apps/helios && npm run build
```
Expected: exit 0, no errors.

i18n key coverage:
```bash
grep -c "summaryTitle\|summaryError\|asOf\|summaryLabels" apps/helios/messages/en.json
grep -c "summaryTitle\|summaryError\|asOf\|summaryLabels" apps/helios/messages/vi.json
```
Expected: both return 4+ (one match per key category).

Component file existence:
```bash
ls apps/helios/src/components/market/market-summary-cards.tsx
```
Expected: file exists.

Market page section order (summary before macro):
```bash
grep -n "summaryTitle\|macroTitle" apps/helios/src/app/market/page.tsx
```
Expected: summaryTitle line number < macroTitle line number.
</verification>

<success_criteria>
- `npm run build` exits 0 with no TypeScript errors
- `market-summary-cards.tsx` exists with 4 cards, 3-row layout (label + value + change), skeleton state
- `market/page.tsx` has Market Summary section before Macro section
- Both locale files have summaryTitle, summaryError, asOf, summaryLabels.{vnindex,totalVolume,advances,breadth}
- `useMarketSummary` in queries.ts uses queryKey ["market", "summary"] and staleTime 30 * 60 * 1000
</success_criteria>

<output>
After completion, create `.planning/phases/17-market-overview-metrics/17-02-SUMMARY.md`
</output>
