---
phase: 16-table-search-session-bar
plan: "05"
type: execute
wave: 3
depends_on:
  - "16-02"
  - "16-03"
  - "16-04"
autonomous: false
files_modified: []
requirements:
  - TBL-01
  - TBL-02
  - TBL-03
  - TBL-04
  - MKT-01
  - MKT-02

must_haves:
  truths:
    - "All four Phase 16 success criteria are verified by user inspection"
    - "All automated tests pass before the user checkpoint begins"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification checkpoint for all Phase 16 work. The user confirms that sort, search, and session bar all behave correctly in the browser before the phase is closed.

Purpose: Phase 16 makes visual changes (sort icons, search bar, session bar) that require a human to confirm correct behavior in the browser.
Output: User approval or a list of issues to fix before phase sign-off.
</objective>

<execution_context>
@/home/toanak/workspace/localstock/.claude/get-shit-done/workflows/execute-plan.md
@/home/toanak/workspace/localstock/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/toanak/workspace/localstock/.planning/ROADMAP.md
@/home/toanak/workspace/localstock/.planning/phases/16-table-search-session-bar/16-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite before checkpoint</name>
  <files></files>
  <read_first>
    - apps/helios/vitest.config.ts (vitest config — confirms test discovery path)
  </read_first>
  <action>
    Run the complete vitest suite to verify all Phase 16 automated tests pass:
    ```bash
    cd /home/toanak/workspace/localstock/apps/helios && npx vitest run --reporter=verbose
    ```

    Expected passing tests:
    - `tests/sort-comparator.test.ts` — 8 tests (numeric sort, grade sort, tiebreaker, recommendation guard)
    - `tests/search-filter.test.ts` — 6 tests (prefix match, case-insensitive, null name, whitespace)
    - `tests/hose-session.test.ts` — 12 tests (timezone conversion, all 7 HOSE phases, weekend/closed)

    If any test fails, fix the failure before proceeding to the visual checkpoint.

    Also verify TypeScript compiles cleanly:
    ```bash
    cd /home/toanak/workspace/localstock/apps/helios && npx tsc --noEmit 2>&1 | head -20
    ```
  </action>
  <verify>
    <automated>cd /home/toanak/workspace/localstock/apps/helios && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npx vitest run` exits with code 0
    - All 26 tests pass (8 sort + 6 search + 12 session)
    - No TypeScript errors (npx tsc --noEmit exits 0)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 16 implemented all three features:

    1. **Table sort fix (TBL-01, TBL-02)** — Numeric sort with symbol tiebreaker, grade semantic sort (A+ first on desc), sort direction icons (ChevronUp/Down), Recommendation column non-sortable.

    2. **Search bar (TBL-03, TBL-04)** — Search input above rankings table filters by symbol prefix or name substring, URL param `?q=` persists search term across navigation, clear button removes the param.

    3. **Market session bar (MKT-01, MKT-02)** — HOSE session progress bar in the app header center showing current phase, % progress, and countdown. Closed state shows time until next opening. Refreshes every 60 seconds.
  </what-built>
  <how-to-verify>
    Start the dev server: `cd /home/toanak/workspace/localstock/apps/helios && npm run dev`

    **Test 1 — Sort fix (TBL-01, TBL-02):**
    1. Open `http://localhost:3000/rankings`
    2. Click the **Score** column header — rows should reorder numerically (highest score first)
    3. Verify a `▼` (ChevronDown) icon appears on the Score header, not on other columns
    4. Click Score header again — rows reverse order (lowest first), icon changes to `▲` (ChevronUp)
    5. Click **Grade** column header (desc) — A+ stocks should appear at the top, C stocks at the bottom
    6. Click **Grade** again (asc) — C stocks appear at the top, A+ at the bottom
    7. Click **Signal** (Recommendation) column header — nothing should happen; no icon, no reorder
    8. If two stocks have the same score, verify they are ordered A→Z by symbol

    **Test 2 — Search bar (TBL-03, TBL-04):**
    1. The rankings page should show a search input above the table
    2. Type `VNM` in the search box — table filters to show only VNM (or stocks starting with VNM)
    3. Check the URL bar — it should read `?q=VNM` (after ~150ms)
    4. Click the × button — input clears, URL param disappears, all stocks reappear
    5. Type `vnm` (lowercase) — same result as VNM (case-insensitive)
    6. Type `ZZZZZZ` (non-existent symbol) — a "No stocks match" message appears
    7. Navigate to a stock page (e.g., click on a stock row), then press Back — search term should be restored in the URL and the input

    **Test 3 — Market Session Bar (MKT-01, MKT-02):**
    1. The header should show a market session indicator in the center (between logo and controls)
    2. If market is currently OPEN (weekday 08:30–14:45 Vietnam time = 01:30–07:45 UTC):
       - A progress bar appears with a phase label (e.g., "Morning") and countdown (e.g., "45m left")
    3. If market is CLOSED (outside those hours):
       - Text shows `● Closed · Opens in Xh Ym` with a countdown to the next opening
    4. On a viewport narrower than 640px (resize browser), the session bar should disappear
    5. Wait up to 1 minute — the countdown should update (or just verify it shows the correct current time)
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 16, or describe any issues found for fixing before sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human review → phase sign-off | Checkpoint prevents shipping broken UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Tampering | Phase sign-off | accept | Visual verification checkpoint ensures broken behavior is caught before phase is marked complete. No security-specific threat in this plan. |
</threat_model>

<verification>
Verification is performed by the user during the checkpoint. Success = user types "approved".
</verification>

<success_criteria>
- `cd apps/helios && npx vitest run` exits 0 before checkpoint begins
- User approves all three features: sort fix, search bar, session bar
- No regressions in existing functionality (navigation, stock detail pages, admin, theme toggle still work)
</success_criteria>

<output>
After completion, create `.planning/phases/16-table-search-session-bar/16-05-SUMMARY.md`
</output>
