---
phase: 04-ai-reports-macro-t3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/localstock/db/models.py
  - src/localstock/db/repositories/macro_repo.py
  - src/localstock/db/repositories/report_repo.py
  - src/localstock/config.py
  - src/localstock/macro/__init__.py
  - src/localstock/reports/__init__.py
  - alembic/versions/add_phase4_macro_report_tables.py
  - tests/test_macro/__init__.py
  - tests/test_reports/__init__.py
  - tests/test_db/test_models_phase4.py
autonomous: true
requirements:
  - MACR-01
  - REPT-01

must_haves:
  truths:
    - "MacroIndicator model stores 4 indicator types (interest_rate, exchange_rate_usd_vnd, cpi, gdp)"
    - "AnalysisReport model stores LLM-generated report with all fields (content_json, summary, recommendation, t3_prediction)"
    - "MacroRepository can bulk_upsert and query latest macro indicators"
    - "ReportRepository can upsert and query reports by symbol and date"
    - "Scoring weight defaults are updated to 0.30/0.30/0.20/0.20"
    - "Alembic migration creates macro_indicators and analysis_reports tables"
  artifacts:
    - path: "src/localstock/db/models.py"
      provides: "MacroIndicator + AnalysisReport ORM models"
      contains: "class MacroIndicator"
    - path: "src/localstock/db/repositories/macro_repo.py"
      provides: "MacroRepository CRUD"
      exports: ["MacroRepository"]
    - path: "src/localstock/db/repositories/report_repo.py"
      provides: "ReportRepository CRUD"
      exports: ["ReportRepository"]
    - path: "src/localstock/config.py"
      provides: "Updated scoring weights + report config"
      contains: "report_top_n"
  key_links:
    - from: "src/localstock/db/repositories/macro_repo.py"
      to: "src/localstock/db/models.py"
      via: "imports MacroIndicator"
      pattern: "from localstock.db.models import MacroIndicator"
    - from: "src/localstock/db/repositories/report_repo.py"
      to: "src/localstock/db/models.py"
      via: "imports AnalysisReport"
      pattern: "from localstock.db.models import AnalysisReport"
---

<objective>
Create the database foundation for Phase 4: MacroIndicator and AnalysisReport models, their repositories, Alembic migration, config updates, and module scaffolds.

Purpose: All Phase 4 modules (macro, reports, T+3) need these DB models and repositories to store their output. Config updates activate the macro scoring dimension.
Output: Two new DB tables, two new repositories, updated scoring weight defaults, module package scaffolds.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-reports-macro-t3/04-RESEARCH.md
@src/localstock/db/models.py
@src/localstock/db/repositories/score_repo.py
@src/localstock/config.py
@alembic/versions/c4007f49f9a7_add_phase3_sentiment_scoring_tables.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/localstock/db/models.py (existing pattern):
```python
class Base(DeclarativeBase):
    pass

# All models use: mapped_column, DateTime(timezone=True), UniqueConstraint
# Date columns use: date (from datetime)
# Timestamps use: datetime.now(UTC) as default
# JSON columns: Mapped[dict] = mapped_column(JSON)
```

From src/localstock/db/repositories/score_repo.py (existing pattern):
```python
class ScoreRepository:
    def __init__(self, session: AsyncSession): ...
    async def bulk_upsert(self, rows: list[dict]) -> int: ...
    async def get_latest(self, symbol: str) -> CompositeScore | None: ...
    async def get_top_ranked(self, target_date, limit) -> list[CompositeScore]: ...
    # Uses pg_insert().on_conflict_do_update() for upserts
```

From src/localstock/config.py (existing pattern):
```python
class Settings(BaseSettings):
    scoring_weight_technical: float = 0.35
    scoring_weight_fundamental: float = 0.35
    scoring_weight_sentiment: float = 0.30
    scoring_weight_macro: float = 0.0  # Phase 4 activates this
    funnel_top_n: int = 50
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB models, migration, repositories</name>
  <files>
    src/localstock/db/models.py,
    src/localstock/db/repositories/macro_repo.py,
    src/localstock/db/repositories/report_repo.py,
    alembic/versions/add_phase4_macro_report_tables.py,
    src/localstock/macro/__init__.py,
    src/localstock/reports/__init__.py,
    tests/test_macro/__init__.py,
    tests/test_reports/__init__.py,
    tests/test_db/test_models_phase4.py
  </files>
  <behavior>
    - Test: MacroIndicator model has fields: id, indicator_type(String(30)), value(Float), period(String(20)), source(String(50)), trend(String(20) nullable), recorded_at(Date), fetched_at(DateTime tz-aware)
    - Test: MacroIndicator has UniqueConstraint on (indicator_type, period) named "uq_macro_indicator"
    - Test: AnalysisReport model has fields: id, symbol(String(10) indexed), date(Date indexed), report_type(String(20)), content_json(JSON), summary(Text), recommendation(String(20)), t3_prediction(String(20) nullable), model_used(String(50)), total_score(Float), grade(String(2)), generated_at(DateTime tz-aware)
    - Test: AnalysisReport has UniqueConstraint on (symbol, date, report_type) named "uq_analysis_report"
    - Test: MacroRepository.bulk_upsert accepts list of dicts and returns count
    - Test: MacroRepository.get_latest_by_type returns most recent indicator for given type
    - Test: MacroRepository.get_all_latest returns latest of each indicator type
    - Test: ReportRepository.upsert stores/updates report row
    - Test: ReportRepository.get_latest returns most recent report for symbol
    - Test: ReportRepository.get_by_date returns all reports for a date
  </behavior>
  <action>
1. **Add MacroIndicator and AnalysisReport models to `src/localstock/db/models.py`:**
   - Follow exact pattern from existing models (DateTime(timezone=True), mapped_column, etc.)
   - MacroIndicator: per research §New Database Models — fields: id (Integer PK auto), indicator_type (String(30)), value (Float), period (String(20)), source (String(50)), trend (String(20) nullable), recorded_at (Date), fetched_at (DateTime(timezone=True) default=now(UTC)). UniqueConstraint("indicator_type", "period", name="uq_macro_indicator").
   - AnalysisReport: per research §New Database Models — fields: id (Integer PK auto), symbol (String(10) index=True), date (Date index=True), report_type (String(20)), content_json (JSON), summary (Text), recommendation (String(20)), t3_prediction (String(20) nullable), model_used (String(50)), total_score (Float), grade (String(2)), generated_at (DateTime(timezone=True) default=now(UTC)). UniqueConstraint("symbol", "date", "report_type", name="uq_analysis_report").
   - Add necessary imports at top of models.py: `Text` is already imported.

2. **Create `src/localstock/db/repositories/macro_repo.py`:**
   - Follow ScoreRepository pattern exactly: __init__(session), pg_insert + on_conflict_do_update.
   - `bulk_upsert(rows: list[dict]) -> int` — upsert on constraint "uq_macro_indicator", update value/source/trend/fetched_at.
   - `get_latest_by_type(indicator_type: str) -> MacroIndicator | None` — select where indicator_type, order by recorded_at desc, limit 1.
   - `get_all_latest() -> list[MacroIndicator]` — for each distinct indicator_type, get the latest recorded_at row. Use subquery with func.max(recorded_at) grouped by indicator_type, then join.

3. **Create `src/localstock/db/repositories/report_repo.py`:**
   - Follow ScoreRepository pattern.
   - `upsert(row: dict) -> None` — pg_insert + on_conflict_do_update on "uq_analysis_report".
   - `get_latest(symbol: str) -> AnalysisReport | None` — latest by generated_at.
   - `get_by_date(target_date: date) -> list[AnalysisReport]` — all reports for date, ordered by total_score desc.
   - `get_by_symbol_and_date(symbol: str, target_date: date) -> AnalysisReport | None` — specific report.

4. **Create Alembic migration:**
   - Run `alembic revision --autogenerate -m "add_phase4_macro_report_tables"` or manually create migration file.
   - Must create `macro_indicators` and `analysis_reports` tables with all columns and constraints.

5. **Create package init files:**
   - `src/localstock/macro/__init__.py` — empty docstring: `"""Macro-economic data collection and impact analysis."""`
   - `src/localstock/reports/__init__.py` — empty docstring: `"""AI report generation and T+3 prediction."""`
   - `tests/test_macro/__init__.py` — empty
   - `tests/test_reports/__init__.py` — empty

6. **Create tests in `tests/test_db/test_models_phase4.py`:**
   - Test model instantiation and field types (no DB needed — test ORM model class).
   - Test unique constraint names exist in __table_args__.
   - Test model_json_schema or column inspection for required fields.
   - Repository tests should mock AsyncSession and verify SQL statement construction.
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localstock && uv run python -m pytest tests/test_db/test_models_phase4.py -x --timeout=30 -v</automated>
  </verify>
  <done>
    - MacroIndicator and AnalysisReport models added to models.py with correct fields and constraints
    - MacroRepository has bulk_upsert, get_latest_by_type, get_all_latest methods
    - ReportRepository has upsert, get_latest, get_by_date, get_by_symbol_and_date methods
    - Alembic migration file exists and creates both tables
    - All 4 package __init__.py files exist
    - Tests pass for model fields, constraints, and repository method signatures
  </done>
</task>

<task type="auto">
  <name>Task 2: Config updates for Phase 4 activation</name>
  <files>src/localstock/config.py</files>
  <action>
1. **Update scoring weight defaults in `src/localstock/config.py`:**
   - Change `scoring_weight_technical` default from `0.35` to `0.30`
   - Change `scoring_weight_fundamental` default from `0.35` to `0.30`
   - Change `scoring_weight_sentiment` default from `0.30` to `0.20`
   - Change `scoring_weight_macro` default from `0.0` to `0.20`
   - The dynamic weight redistribution in compute_composite() already handles when macro data is unavailable (macro=None → weights redistribute to other dims). So changing defaults is safe.

2. **Add report generation settings:**
   - `report_top_n: int = 20` — number of top-ranked stocks to generate reports for (per research: funnel pattern, 20 stocks max)
   - `report_max_tokens: int = 4096` — LLM context window for report generation

3. **Do NOT reset the lru_cache** — the `get_settings()` function caches on first call, which is correct behavior.
  </action>
  <verify>
    <automated>cd /home/toanak/playground/localstock && uv run python -c "from localstock.config import Settings; s = Settings(); assert s.scoring_weight_macro == 0.20; assert s.report_top_n == 20; print('Config OK')"</automated>
  </verify>
  <done>
    - Scoring weight defaults are 0.30/0.30/0.20/0.20 (activating macro dimension)
    - report_top_n=20 and report_max_tokens=4096 settings exist
    - Existing tests still pass (weight redistribution handles missing dimensions)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → MacroRepository | Macro data input via POST endpoint (Plan 4) |
| LLM output → AnalysisReport | LLM-generated content stored in DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | MacroIndicator.value | mitigate | Pydantic validation on API input in Plan 4; indicator_type constrained to 4 known values |
| T-04-02 | Information Disclosure | AnalysisReport.content_json | accept | Single-user tool; report content is analysis, not PII |
</threat_model>

<verification>
- All existing 147 tests still pass: `uv run python -m pytest tests/ --timeout=30`
- New model tests pass: `uv run python -m pytest tests/test_db/test_models_phase4.py -x --timeout=30`
- Config imports cleanly with new defaults
- Module packages importable: `python -c "import localstock.macro; import localstock.reports"`
</verification>

<success_criteria>
- MacroIndicator and AnalysisReport models are in models.py with all fields from research spec
- Repositories follow established pg_insert upsert pattern
- Alembic migration file exists
- Scoring weight defaults updated to 0.30/0.30/0.20/0.20
- Report config settings added
- All module scaffolds created
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-reports-macro-t3/04-01-SUMMARY.md`
</output>
