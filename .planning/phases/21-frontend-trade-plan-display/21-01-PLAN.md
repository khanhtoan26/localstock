---
phase: 21-frontend-trade-plan-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/helios/src/lib/types.ts
  - apps/helios/messages/vi.json
  - apps/helios/messages/en.json
  - apps/helios/src/components/stock/trade-plan-section.tsx
  - apps/helios/tests/trade-plan.test.ts
autonomous: true
requirements: [FRONTEND-01, FRONTEND-02, FRONTEND-03]

must_haves:
  truths:
    - "TradePlanData interface exists with entry_price, stop_loss, target_price, risk_rating, signal_conflicts, catalyst fields"
    - "extractTradePlan() returns null when all price fields are null (pre-v1.4 backward compat)"
    - "extractTradePlan() returns typed TradePlanData from content_json with runtime narrowing"
    - "TradePlanSection renders skeleton when loading, null when no data, full card when data present"
    - "Risk badge shows correct color class for high/medium/low with tooltip"
    - "Signal conflict alert renders only when signal_conflicts is non-null"
    - "All user-facing text uses next-intl translation keys"
  artifacts:
    - path: "apps/helios/src/lib/types.ts"
      provides: "TradePlanData interface"
      contains: "TradePlanData"
    - path: "apps/helios/src/components/stock/trade-plan-section.tsx"
      provides: "TradePlanSection component with extractTradePlan, RiskBadge, PriceLevelRow, SignalConflictAlert"
      exports: ["TradePlanSection"]
    - path: "apps/helios/tests/trade-plan.test.ts"
      provides: "Unit tests for extractTradePlan logic"
    - path: "apps/helios/messages/vi.json"
      provides: "Vietnamese i18n keys for stock.tradePlan"
      contains: "tradePlan"
    - path: "apps/helios/messages/en.json"
      provides: "English i18n keys for stock.tradePlan"
      contains: "tradePlan"
  key_links:
    - from: "apps/helios/src/components/stock/trade-plan-section.tsx"
      to: "apps/helios/src/lib/types.ts"
      via: "import type { TradePlanData, StockReport }"
      pattern: "import.*TradePlanData"
    - from: "apps/helios/src/components/stock/trade-plan-section.tsx"
      to: "apps/helios/src/lib/utils.ts"
      via: "import { formatVND }"
      pattern: "formatVND"
---

<objective>
Create the TradePlanSection component with all sub-components, types, i18n keys, and unit tests.

Purpose: Build the complete Trade Plan UI component that extracts trade plan data from content_json and renders price levels, risk badge with tooltip, and conditional signal conflict alert — ready for page integration in Plan 02.
Output: New TradePlanSection component, TradePlanData type, i18n keys (vi+en), unit tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-frontend-trade-plan-display/21-CONTEXT.md
@.planning/phases/21-frontend-trade-plan-display/21-RESEARCH.md
@.planning/phases/21-frontend-trade-plan-display/21-PATTERNS.md

@apps/helios/src/lib/types.ts
@apps/helios/src/lib/utils.ts
@apps/helios/src/components/stock/ai-report-panel.tsx
@apps/helios/src/components/stock/score-breakdown.tsx
@apps/helios/src/components/rankings/grade-badge.tsx
@apps/helios/src/components/ui/tooltip.tsx
@apps/helios/src/components/ui/card.tsx
@apps/helios/src/components/ui/skeleton.tsx
@apps/helios/messages/vi.json
@apps/helios/messages/en.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/helios/src/lib/types.ts:
```typescript
export interface StockReport {
  symbol: string;
  report_type?: string;
  content_json: Record<string, unknown> | null;
  summary: string | null;
  recommendation: string | null;
  t3_prediction: string | null;
  total_score: number | null;
  grade: string | null;
  model_used?: string;
  generated_at: string;
}
```

From apps/helios/src/lib/utils.ts:
```typescript
export function formatVND(value: number | null | undefined): string;
export const gradeColors: Record<string, string>;
```

From apps/helios/src/components/ui/tooltip.tsx:
```typescript
// 5-part composition from @base-ui/react:
export { Tooltip, TooltipTrigger, TooltipPortal, TooltipPositioner, TooltipContent }
// Usage:
// <Tooltip>
//   <TooltipTrigger>{element}</TooltipTrigger>
//   <TooltipPortal>
//     <TooltipPositioner>
//       <TooltipContent>{text}</TooltipContent>
//     </TooltipPositioner>
//   </TooltipPortal>
// </Tooltip>
```

From apps/helios/src/components/rankings/grade-badge.tsx:
```typescript
export function GradeBadge({ grade }: { grade: string }) {
  const colors = gradeColors[grade] || gradeColors.F;
  return (
    <span className={`px-2 py-0.5 rounded text-xs font-bold border ${colors}`}>
      {grade}
    </span>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TradePlanData type and i18n keys</name>
  <files>apps/helios/src/lib/types.ts, apps/helios/messages/vi.json, apps/helios/messages/en.json</files>
  <action>
**types.ts** — Add `TradePlanData` interface after the existing `StockReport` interface (after line 115):

```typescript
/** Trade plan fields extracted from content_json (v1.4+). All null for pre-v1.4 reports. */
export interface TradePlanData {
  entry_price: number | null;
  stop_loss: number | null;
  target_price: number | null;
  risk_rating: "high" | "medium" | "low" | null;
  signal_conflicts: string | null;
  catalyst: string | null;
}
```

**vi.json** — Add `tradePlan` key inside the `"stock"` object, after the existing `"score"` block (after the closing `}` of `"score"`):

```json
"tradePlan": {
  "title": "Kế Hoạch Giao Dịch",
  "entryZone": "Vùng vào lệnh",
  "stopLoss": "Cắt lỗ",
  "targetPrice": "Giá mục tiêu",
  "riskLevel": "Mức rủi ro",
  "riskHigh": "Cao",
  "riskMedium": "Trung bình",
  "riskLow": "Thấp",
  "signalConflict": "Xung đột tín hiệu",
  "catalyst": "Chất xúc tác"
}
```

**en.json** — Add matching `tradePlan` key inside the `"stock"` object at the same position:

```json
"tradePlan": {
  "title": "Trade Plan",
  "entryZone": "Entry Zone",
  "stopLoss": "Stop-Loss",
  "targetPrice": "Target Price",
  "riskLevel": "Risk Level",
  "riskHigh": "High",
  "riskMedium": "Medium",
  "riskLow": "Low",
  "signalConflict": "Signal Conflict",
  "catalyst": "Catalyst"
}
```

Note: Do NOT include emoji ⚠️ in i18n keys — the AlertCircle icon handles the visual indicator (per D-06).
  </action>
  <verify>
    <automated>cd apps/helios && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TradePlanData interface exported from types.ts; tradePlan i18n keys in both vi.json and en.json inside the stock namespace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create extractTradePlan helper tests</name>
  <files>apps/helios/tests/trade-plan.test.ts</files>
  <behavior>
    - Test: extractTradePlan returns null when content_json is null
    - Test: extractTradePlan returns null when all price fields are missing (pre-v1.4 report)
    - Test: extractTradePlan returns null when all price fields are explicitly null
    - Test: extractTradePlan returns valid TradePlanData when entry_price, stop_loss, target_price are numbers
    - Test: extractTradePlan handles partial data (only some price fields present)
    - Test: extractTradePlan type-narrows risk_rating to only "high"|"medium"|"low"|null (rejects invalid strings)
    - Test: extractTradePlan type-narrows signal_conflicts to string|null (rejects non-string values)
    - Test: getRiskColors returns correct color class for each rating
  </behavior>
  <action>
Create `apps/helios/tests/trade-plan.test.ts`. Import `extractTradePlan` and `getRiskColors` from `@/components/stock/trade-plan-section` (they will be exported for testing).

```typescript
import { describe, it, expect } from "vitest";
import { extractTradePlan, getRiskColors } from "@/components/stock/trade-plan-section";

describe("extractTradePlan", () => {
  it("returns null when content_json is null", () => {
    expect(extractTradePlan(null)).toBeNull();
  });

  it("returns null when all price fields are missing (pre-v1.4)", () => {
    expect(extractTradePlan({ summary: "old report" })).toBeNull();
  });

  it("returns null when all price fields are explicitly null", () => {
    expect(extractTradePlan({
      entry_price: null,
      stop_loss: null,
      target_price: null,
    })).toBeNull();
  });

  it("returns TradePlanData when price fields are numbers", () => {
    const result = extractTradePlan({
      entry_price: 45200,
      stop_loss: 43000,
      target_price: 52000,
      risk_rating: "medium",
      signal_conflicts: "RSI overbought vs MACD bullish",
      catalyst: "Q4 earnings beat",
    });
    expect(result).toEqual({
      entry_price: 45200,
      stop_loss: 43000,
      target_price: 52000,
      risk_rating: "medium",
      signal_conflicts: "RSI overbought vs MACD bullish",
      catalyst: "Q4 earnings beat",
    });
  });

  it("handles partial price data (only entry_price present)", () => {
    const result = extractTradePlan({ entry_price: 45200 });
    expect(result).not.toBeNull();
    expect(result!.entry_price).toBe(45200);
    expect(result!.stop_loss).toBeNull();
    expect(result!.target_price).toBeNull();
  });

  it("rejects invalid risk_rating values", () => {
    const result = extractTradePlan({
      entry_price: 45200,
      risk_rating: "extreme",
    });
    expect(result!.risk_rating).toBeNull();
  });

  it("rejects non-string signal_conflicts", () => {
    const result = extractTradePlan({
      entry_price: 45200,
      signal_conflicts: 12345,
    });
    expect(result!.signal_conflicts).toBeNull();
  });
});

describe("getRiskColors", () => {
  it("returns red classes for high", () => {
    expect(getRiskColors("high")).toContain("red");
  });

  it("returns amber/yellow classes for medium", () => {
    const colors = getRiskColors("medium");
    expect(colors.includes("yellow") || colors.includes("amber")).toBe(true);
  });

  it("returns green classes for low", () => {
    expect(getRiskColors("low")).toContain("green");
  });

  it("returns empty string for unknown rating", () => {
    expect(getRiskColors("unknown")).toBe("");
  });
});
```

Write tests FIRST (RED). They will fail until Task 3 creates the component with exports.
  </action>
  <verify>
    <automated>cd apps/helios && npx vitest run tests/trade-plan.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Test file exists with 8+ test cases covering extractTradePlan null handling, valid extraction, type narrowing, and getRiskColors mapping.</done>
</task>

<task type="auto">
  <name>Task 3: Create TradePlanSection component</name>
  <files>apps/helios/src/components/stock/trade-plan-section.tsx</files>
  <action>
Create `apps/helios/src/components/stock/trade-plan-section.tsx` as a `"use client"` component.

**Structure — export these for testing:**
- `extractTradePlan(contentJson: Record<string, unknown> | null): TradePlanData | null` — type-narrows each field from content_json. Returns null if ALL of entry_price, stop_loss, target_price are null/missing (D-09 backward compat).
- `getRiskColors(rating: string): string` — maps risk rating to Tailwind classes.

**Internal sub-components (not exported):**
- `RiskBadge` — pill badge with color from getRiskColors, wrapped in Tooltip showing report summary as reasoning (D-03, D-04). Use `Tooltip > TooltipTrigger > TooltipPortal > TooltipPositioner > TooltipContent` from `@/components/ui/tooltip`.
- `PriceLevelRow` — renders label + `formatVND(price)` + percentage variance from currentClose in muted text (D-07, D-08). If price is null, row is not rendered. Percentage: `((price - currentClose) / currentClose) * 100`, formatted as `(+X.X%)` or `(-X.X%)`.
- `SignalConflictAlert` — amber alert box with `AlertTriangle` icon from lucide-react (not emoji), showing signal conflict text verbatim (D-05, D-06). Use `AlertTriangle` not `AlertCircle` — triangles are standard for warnings.

**Main component:**
```typescript
interface TradePlanSectionProps {
  report: StockReport | undefined;
  isLoading: boolean;
  currentClose: number | null;
}

export function TradePlanSection({ report, isLoading, currentClose }: TradePlanSectionProps)
```

**Rendering logic:**
1. `const t = useTranslations("stock.tradePlan");`
2. If `isLoading` → return skeleton Card with `<Skeleton>` elements matching existing pattern (D-10).
3. Extract trade plan: `const tradePlan = extractTradePlan(report?.content_json ?? null);`
4. If `!tradePlan` → return `null` (D-09, completely hidden).
5. Render `<Card>` with:
   - `<CardHeader>` with `<CardTitle>{t("title")}</CardTitle>` and optional `<RiskBadge>` next to title if `tradePlan.risk_rating` is non-null.
   - `<CardContent>` with:
     - 3 `<PriceLevelRow>` instances for entry_price (label: `t("entryZone")`), stop_loss (label: `t("stopLoss")`), target_price (label: `t("targetPrice")`).
     - Optional catalyst row: if `tradePlan.catalyst` is non-null, render a small muted text line with `t("catalyst")` label and catalyst text.
     - Conditional `<SignalConflictAlert text={tradePlan.signal_conflicts}>` only when `signal_conflicts` is non-null (D-05).

**Risk badge color map** — follow gradeColors pattern from utils.ts:
```typescript
const riskColors: Record<string, string> = {
  high:   "bg-red-500/20 text-red-700 dark:text-red-400 border-red-500/30",
  medium: "bg-yellow-500/20 text-yellow-700 dark:text-yellow-400 border-yellow-500/30",
  low:    "bg-green-500/20 text-green-700 dark:text-green-400 border-green-500/30",
};
```

**Risk badge labels** — use i18n keys: `t("riskHigh")`, `t("riskMedium")`, `t("riskLow")`. Map: `{ high: "riskHigh", medium: "riskMedium", low: "riskLow" }`.

**Tooltip content** — use `report.summary` as the reasoning text source (D-04). If summary is null, don't wrap in tooltip.

**Percentage variance coloring** — use existing up/down pattern from page.tsx:
- Positive: `text-green-600 dark:text-green-400`
- Negative: `text-red-600 dark:text-red-400`

**Signal conflict alert styling:**
```typescript
<div className="flex gap-2 rounded-lg border border-yellow-500/30 bg-yellow-500/10 p-3 mt-3">
  <AlertTriangle className="h-4 w-4 text-yellow-600 dark:text-yellow-400 mt-0.5 shrink-0" />
  <div>
    <p className="text-xs font-semibold text-yellow-700 dark:text-yellow-300">{t("signalConflict")}</p>
    <p className="text-xs text-yellow-600 dark:text-yellow-400 mt-1">{text}</p>
  </div>
</div>
```

**Imports:**
```typescript
"use client";
import { useTranslations } from "next-intl";
import { AlertTriangle } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";
import { Tooltip, TooltipTrigger, TooltipPortal, TooltipPositioner, TooltipContent } from "@/components/ui/tooltip";
import { formatVND } from "@/lib/utils";
import type { StockReport, TradePlanData } from "@/lib/types";
```
  </action>
  <verify>
    <automated>cd apps/helios && npx vitest run tests/trade-plan.test.ts --reporter=verbose 2>&1 | tail -25</automated>
  </verify>
  <done>All 8+ tests in trade-plan.test.ts pass (GREEN). TradePlanSection component renders: skeleton when loading, null when no data, full card with price levels + risk badge + conditional signal conflict when data present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Client (content_json) | Untrusted JSON blob from server — shape may not match expected fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | extractTradePlan | mitigate | Runtime type narrowing for every field — `typeof x === "number"`, `typeof x === "string"`, array inclusion check for risk_rating. Never trust content_json shape. |
| T-21-02 | Tampering | SignalConflictAlert | mitigate | React auto-escapes JSX text content. Do NOT use dangerouslySetInnerHTML for signal_conflicts text. Render as plain `{text}` in `<p>` element. |
| T-21-03 | Information Disclosure | Tooltip content | accept | Report summary is already visible in AI Report panel — showing it in tooltip doesn't expose new data. |
</threat_model>

<verification>
```bash
# All unit tests pass
cd apps/helios && npx vitest run tests/trade-plan.test.ts --reporter=verbose

# TypeScript compiles without errors
cd apps/helios && npx tsc --noEmit

# i18n keys present
grep -q "tradePlan" apps/helios/messages/vi.json && grep -q "tradePlan" apps/helios/messages/en.json && echo "i18n OK"
```
</verification>

<success_criteria>
- TradePlanData interface exported from types.ts
- extractTradePlan() returns null for pre-v1.4 reports (all prices null)
- extractTradePlan() returns typed data with runtime narrowing for v1.4+ reports
- getRiskColors() maps high→red, medium→yellow, low→green
- TradePlanSection component handles loading (skeleton), no data (null), and full render states
- Signal conflict alert only renders when signal_conflicts is non-null
- All 8+ unit tests pass
- TypeScript compiles cleanly
- i18n keys in both vi.json and en.json
</success_criteria>

<output>
After completion, create `.planning/phases/21-frontend-trade-plan-display/21-01-SUMMARY.md`
</output>
