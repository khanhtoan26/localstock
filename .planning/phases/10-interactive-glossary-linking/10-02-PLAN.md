---
phase: 10-interactive-glossary-linking
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/helios/src/components/stock/ai-report-panel.tsx
autonomous: false
requirements: [GLOSS-01, GLOSS-03]

must_haves:
  truths:
    - "AI report text in stock page shows glossary terms with dotted underline highlights"
    - "Hovering a highlighted term shows a preview card with term definition"
    - "Clicking 'Xem chi tiết →' in hover card navigates to /learn/[category]#[term-id]"
    - "Only the first occurrence of each term is highlighted in the report"
  artifacts:
    - path: "apps/helios/src/components/stock/ai-report-panel.tsx"
      provides: "AI report rendering with glossary-linked terms"
      contains: "GlossaryMarkdown"
  key_links:
    - from: "apps/helios/src/components/stock/ai-report-panel.tsx"
      to: "apps/helios/src/components/glossary/glossary-markdown.tsx"
      via: "import and render GlossaryMarkdown"
      pattern: "import.*GlossaryMarkdown.*from.*@/components/glossary/glossary-markdown"
    - from: "apps/helios/src/components/glossary/glossary-term.tsx"
      to: "/learn/{category}#{id}"
      via: "Next.js Link click-through"
      pattern: "href=.*learn/"
---

<objective>
Wire the GlossaryMarkdown component into the AI report panel, activating glossary term linking in live AI reports, and verify the complete interactive flow visually.

Purpose: This plan activates the glossary linking capability built in Plan 01 by replacing the plain `<Markdown>` component with `<GlossaryMarkdown>` in the AI report panel. Without this wiring, the linking components exist but are never used.
Output: Modified `ai-report-panel.tsx` with glossary-linked rendering + visual verification of the full flow.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-interactive-glossary-linking/10-CONTEXT.md
@.planning/phases/10-interactive-glossary-linking/10-UI-SPEC.md
@.planning/phases/10-interactive-glossary-linking/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor should verify these exist before wiring -->

From apps/helios/src/components/glossary/glossary-markdown.tsx:
```typescript
export function GlossaryMarkdown({ content }: { content: string }): JSX.Element;
// Drop-in replacement for <Markdown>{content}</Markdown>
// Usage: <GlossaryMarkdown content={content} />
```

From apps/helios/src/components/stock/ai-report-panel.tsx (current state):
```typescript
// Line 3: import Markdown from "react-markdown";
// Line 39-41:
// <div className="prose dark:prose-invert prose-sm max-w-none">
//   <Markdown>{content}</Markdown>
// </div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Markdown with GlossaryMarkdown in AI report panel</name>
  <files>apps/helios/src/components/stock/ai-report-panel.tsx</files>
  <read_first>
    - apps/helios/src/components/stock/ai-report-panel.tsx (current state — see exact import and usage lines)
    - apps/helios/src/components/glossary/glossary-markdown.tsx (verify it exists and confirm the export name and props)
  </read_first>
  <action>
Modify `apps/helios/src/components/stock/ai-report-panel.tsx` with these exact changes:

1. **Replace the Markdown import** (line 3):
   - Remove: `import Markdown from "react-markdown";`
   - Add: `import { GlossaryMarkdown } from "@/components/glossary/glossary-markdown";`

2. **Replace the Markdown component usage** (around line 40):
   - Remove: `<Markdown>{content}</Markdown>`
   - Add: `<GlossaryMarkdown content={content} />`

3. **Keep everything else unchanged** — the `prose dark:prose-invert prose-sm max-w-none` wrapper div stays. The loading skeleton, error states, and fallback JSON rendering remain untouched.

The resulting render block should look like:
```tsx
return content ? (
  <div className="prose dark:prose-invert prose-sm max-w-none">
    <GlossaryMarkdown content={content} />
  </div>
) : fallbackJson ? (
  // ... existing fallback JSON rendering unchanged
```

**Why GlossaryMarkdown instead of Markdown:** GlossaryMarkdown internally renders `<Markdown>` with the `components` prop that overrides `p`, `li`, `td`, `th` to scan text for glossary terms and render them as interactive `GlossaryTerm` popover triggers. It's a drop-in wrapper — same output for non-glossary text, enhanced output for text containing glossary terms.
  </action>
  <verify>
    <automated>cd apps/helios && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/helios/src/components/stock/ai-report-panel.tsx` contains `import { GlossaryMarkdown } from "@/components/glossary/glossary-markdown"`
    - File contains `<GlossaryMarkdown content={content} />`
    - File does NOT contain `import Markdown from "react-markdown"` (removed — Markdown is now used internally by GlossaryMarkdown)
    - File does NOT contain `<Markdown>{content}</Markdown>` (replaced)
    - File still contains `prose dark:prose-invert prose-sm max-w-none` (wrapper div preserved)
    - File still contains `Chưa có báo cáo` (error state preserved)
    - File still contains `Báo cáo không có nội dung` (empty state preserved)
    - File still contains `fallbackJson` (JSON fallback preserved)
    - `npm run build` in apps/helios succeeds with no TypeScript errors
  </acceptance_criteria>
  <done>ai-report-panel.tsx uses GlossaryMarkdown instead of plain Markdown. Build passes. Glossary linking is now active for all AI report text rendered on stock pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of glossary linking in AI report</name>
  <files>apps/helios/src/components/stock/ai-report-panel.tsx</files>
  <action>
Human visual verification of the complete glossary linking flow. No code changes — this task validates the work done in Task 1 and Plan 01.

What was built: Complete interactive glossary linking in AI reports — terms are highlighted with dotted underlines, hover cards show definitions, click-through navigates to learn pages.
  </action>
  <verify>
    <automated>cd apps/helios && npx next build 2>&1 | tail -3</automated>
  </verify>
  <how-to-verify>
1. Start dev server: `cd apps/helios && npm run dev`
2. Open http://localhost:3000 in browser
3. Navigate to any stock page that has an AI report (e.g., `/stock/VNM` or any stock with a generated report)

**GLOSS-01 — Auto-linking:**
4. Verify that glossary terms in the AI report text (RSI, MACD, P/E, EMA, etc.) have dotted underlines in the accent/blue color
5. Verify that ONLY the FIRST occurrence of each term is highlighted — subsequent mentions of the same term should appear as normal text
6. Verify that terms inside code blocks (if any) are NOT highlighted

**GLOSS-02 — Hover card:**
7. Hover over a dotted-underline term (e.g., "RSI") — after ~200ms delay, a hover card should appear
8. Verify card shows: Vietnamese term name (bold), short definition (muted text), formula if applicable (monospace block), and "Xem chi tiết →" link
9. Move mouse from term to hover card — card should stay visible (hover bridge)
10. Move mouse away from both — card should dismiss after ~300ms

**GLOSS-04 — Longest-first matching:**
11. If report contains "chỉ số RSI" — verify the ENTIRE phrase is highlighted as one link, not "RSI" separately

**D-03 — Visual style:**
12. Verify dotted underline uses accent/blue color, not text color
13. Hover over a term — underline should change from dotted to solid

**GLOSS-03 — Click-through:**
14. Click "Xem chi tiết →" in the hover card
15. Verify navigation goes to `/learn/[category]#[term-id]` (e.g., `/learn/technical#rsi`)
16. Verify the matching entry on the learn page is auto-scrolled into view and auto-expanded

**Dark mode:**
17. Toggle to dark theme — verify hover card and dotted underlines look correct on dark background

**Mobile (resize browser to mobile width):**
18. Tap a highlighted term — hover card should appear
19. Tap outside — card should dismiss
  </how-to-verify>
  <done>Human has visually verified all GLOSS requirements: auto-linking with dotted underlines, hover card previews, click-through to learn pages, longest-first matching, first-occurrence-only behavior, and dark mode compatibility.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI report text → GlossaryMarkdown | AI-generated markdown flows through react-markdown (sanitized) then glossary scanner (text-only matching) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | AI report text with malicious content | accept | react-markdown sanitizes HTML by default. GlossaryMarkdown does not use `dangerouslySetInnerHTML`. Text scanning only processes string content, not HTML. Personal tool with local LLM — no external input vector. |
</threat_model>

<verification>
1. `npm run build` passes with the modified ai-report-panel.tsx
2. ai-report-panel.tsx imports and uses GlossaryMarkdown instead of Markdown
3. Visual verification confirms all GLOSS requirements working end-to-end
</verification>

<success_criteria>
AI report text on stock pages renders with interactive glossary term highlights. Hovering shows preview cards. Click-through navigates to learn pages with auto-scroll and auto-expand. First-occurrence-only and longest-first matching visually confirmed. Human approves the visual experience.
</success_criteria>

<output>
After completion, create `.planning/phases/10-interactive-glossary-linking/10-02-SUMMARY.md`
</output>
